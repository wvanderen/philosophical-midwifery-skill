---
phase: 02-structured-session-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - philosophical-midwifery/references/SESSION_SCHEMA.md
  - philosophical-midwifery/assets/session_summary_template.md
  - philosophical-midwifery/assets/belief_graph_template.json
  - philosophical-midwifery/scripts/validate_session_schema.py
  - philosophical-midwifery/scripts/fixtures/valid.json
  - philosophical-midwifery/scripts/fixtures/invalid.json
autonomous: false
requirements: [MTH-04, ART-01, ART-02, ART-03]
tags: [agent-skill, session-schema, json-validator, belief-graph, markdown, python-stdlib]
user_setup: []

must_haves:
  truths:
    # SC1 — SESSION_SCHEMA.md (MTH-04)
    - "SESSION_SCHEMA.md documents every field from design-draft §9 (session_id, created_at, phase, presenting_disturbance, concrete_example, candidate_beliefs, selected_belief, normalized_proposition, definitions, implications, counterexamples, contradictions, revised_proposition, unresolved_tensions, next_questions, session_summary) with type + required/optional + description."
    - "SESSION_SCHEMA.md lists the canonical 5-value phase enum (intake, extraction, normalization, examination, synthesis) and explicitly documents contradiction/revision and session-summary as sub-activities of synthesis."
    # SC2 — session_summary_template.md (ART-01)
    - "session_summary_template.md captures the full ART-01 content set (presenting disturbance, concrete example, candidate pathologos, examined proposition, definitions, implications, counterexamples, contradiction/weakness, revised proposition, unresolved tension, next inquiry target) without asserting certainty beyond the conversation."
    # SC3 — belief_graph_template.json (ART-02)
    - "belief_graph_template.json is a future-compatible property-graph seed that remains optional for v1 (signals optionality via header markers)."
    # SC4 — validate_session_schema.py (ART-03)
    - "validate_session_schema.py validates representative session JSON and reports missing/invalid fields clearly in both CLI-prose (default) and --json modes."
    # SC5 — fixtures
    - "At least one valid and one invalid fixture demonstrate validator behavior; both derive from the 'recognition determines worth' thread."
    # Cross-file coherence
    - "The belief-graph worked example and BOTH fixtures derive from the same 'recognition determines worth' thread, so cross-references stay coherent."
    - "Phase 1's four shipped files are NOT modified — Phase 2 is purely additive (per D-08)."
  artifacts:
    - path: "philosophical-midwifery/references/SESSION_SCHEMA.md"
      provides: "Canonical session-state field definitions + 5-value phase enum; single source of truth for the validator (A-2)"
      contains: "phase"
      min_lines: 80
    - path: "philosophical-midwifery/assets/session_summary_template.md"
      provides: "Final inquiry artifact template (render target for the schema's session_summary field, per A-3)"
      contains: "Session Summary"
      min_lines: 40
    - path: "philosophical-midwifery/assets/belief_graph_template.json"
      provides: "Property-graph seed: skeleton schema + ONE recognition-determines-worth worked example"
      contains: "_v1_optional"
    - path: "philosophical-midwifery/scripts/validate_session_schema.py"
      provides: "Stdlib-only session-JSON validator: required + type + enum checks, collect-all reporting, CLI + --json output"
      contains: "import sys"
    - path: "philosophical-midwifery/scripts/fixtures/valid.json"
      provides: "A complete, schema-valid session JSON derived from the recognition-determines-worth thread"
      contains: "recognition"
    - path: "philosophical-midwifery/scripts/fixtures/invalid.json"
      provides: "An invalid session JSON seeding >=2 distinct error classes (missing required field + bad phase value)"
      contains: "phase"
  key_links:
    - from: "philosophical-midwifery/scripts/validate_session_schema.py"
      to: "philosophical-midwifery/references/SESSION_SCHEMA.md"
      via: "validator encodes the schema's required/type/enum rules directly (A-2); the phase enum in the script MUST match the enum documented in SESSION_SCHEMA.md"
      pattern: "intake.*extraction.*normalization.*examination.*synthesis"
    - from: "philosophical-midwifery/assets/session_summary_template.md"
      to: "philosophical-midwifery/references/SESSION_SCHEMA.md"
      via: "the rendered summary-template markdown string populates the schema's session_summary field (A-3); template headings map to schema field names"
      pattern: "Examined proposition"
    - from: "philosophical-midwifery/assets/belief_graph_template.json"
      to: "philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md"
      via: "belief nodes carry optional pathologos_pattern referencing the 4 Phase 1 archetypes (D-07)"
      pattern: "conditional_worth|control_responsibility|belonging_rejection|perfection_flawlessness"
    - from: "philosophical-midwifery/scripts/fixtures/valid.json"
      to: "philosophical-midwifery/scripts/fixtures/invalid.json"
      via: "both fixtures derive from the same recognition-determines-worth thread (D-04); invalid.json is a deliberately-broken sibling of valid.json"
      pattern: "recognition"
---

## Phase Goal

**As a** maintainer/integrator of the philosophical-midwifery skill, **I want to** ship a structured schema, a summary template, a belief-graph seed, and a validator with fixtures, **so that** session outputs become consistent, inspectable, and mechanically checkable — without touching the Phase 1 skill package.

<objective>
Deliver the four structured-artifact deliverables (plus two fixtures) that make Phase 1's inquiry loop produce inspectable outputs: a canonical session schema, a human-readable summary template, a future-compatible belief-graph seed, and a stdlib-only validator with one valid and one invalid fixture — all threaded by the shared "recognition determines worth" example, all purely additive to the Phase 1 package.

Purpose: Phase 1 shipped the skill's behavior. Phase 2 ships the artifact layer that makes that behavior's *outputs* consistent and mechanically checkable, and that Phase 3 (EVAL-02/03) and v2 (HAR-*/GRPH-*) will build on. Every decision (D-01..D-08, A-1..A-4) is pre-locked in CONTEXT.md; this plan executes them without re-derivation.

Output: 6 new files under `philosophical-midwifery/` (references/ + assets/ + scripts/), no edits to Phase 1 files.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-structured-session-artifacts/02-CONTEXT.md
@.planning/phases/01-core-skill-prototype/01-01-SUMMARY.md
@docs/design-draft.md
@philosophical-midwifery/SKILL.md
@philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md
</context>

## Multi-Source Coverage Audit

| Source | Item | Covered By | Status |
|--------|------|-----------|--------|
| GOAL | Schema/templates/validator for consistent, inspectable session artifacts | Task 1 + Task 2 | COVERED |
| REQ | MTH-04 (session schema) | Task 1 → SESSION_SCHEMA.md | COVERED |
| REQ | ART-01 (summary template) | Task 1 → session_summary_template.md | COVERED |
| REQ | ART-02 (belief-graph template) | Task 1 → belief_graph_template.json | COVERED |
| REQ | ART-03 (validate session JSON locally) | Task 2 → validate_session_schema.py + fixtures | COVERED |
| RESEARCH | (skipped — CONTEXT.md self-contained; A-1..A-4 pre-resolve tradeoffs) | n/a | N/A |
| CONTEXT | D-01 stdlib + required/type/enum | Task 2 action cites D-01 | COVERED |
| CONTEXT | D-02 CLI + --json dual output | Task 2 action cites D-02 | COVERED |
| CONTEXT | D-03 collect-all error reporting | Task 2 action cites D-03 | COVERED |
| CONTEXT | D-04 fixtures location + shared example + >=2 error classes | Task 2 action cites D-04 | COVERED |
| CONTEXT | D-05 skeleton + ONE worked example | Task 1 action cites D-05 | COVERED |
| CONTEXT | D-06 property-graph node/edge shapes | Task 1 action cites D-06 | COVERED |
| CONTEXT | D-07 pathologos_pattern on belief nodes | Task 1 action cites D-07 | COVERED |
| CONTEXT | D-08 _v1_optional + _note header; Phase 1 untouched | Task 1 action cites D-08; must_haves truth asserts Phase 1 untouched | COVERED |
| CONTEXT | A-1 5-value enum + synthesis sub-activities | Task 1 action cites A-1 | COVERED |
| CONTEXT | A-2 SESSION_SCHEMA.md single source of truth | Task 1 + Task 2 cite A-2 | COVERED |
| CONTEXT | A-3 summary template → session_summary string; validator non-empty-string check only | Task 1 + Task 2 cite A-3 | COVERED |
| CONTEXT | A-4 CLI shape, ISO-8601 dates, belief_N id convention | Task 2 action cites A-4 | COVERED |

No unplanned items. Deferred items (GRPH recurrence machinery, HAR persistence, JSON-Schema sidecar, behavioral fixtures) are correctly excluded — they belong to v2 or Phase 3.

<tasks>

<task type="auto">
  <name>Task 1: Author the artifact layer — SESSION_SCHEMA.md, session_summary_template.md, belief_graph_template.json</name>
  <files>
    philosophical-midwifery/references/SESSION_SCHEMA.md
    philosophical-midwifery/assets/session_summary_template.md
    philosophical-midwifery/assets/belief_graph_template.json
  </files>
  <read_first>
    - docs/design-draft.md (§9 State Schema = field source; §7 Phase 6 = summary template source; §8 Later Product Architecture = graph node types; §3 worked example; §14 examined-proposition atomic-unit principle)
    - .planning/phases/02-structured-session-artifacts/02-CONTEXT.md (D-05/D-06/D-07/D-08, A-1/A-2/A-3, specifics on the worked example trace)
    - .planning/phases/01-core-skill-prototype/01-CONTEXT.md (D-06 four archetypes, D-09/D-10 voice split, D-12 shared example)
    - .planning/phases/01-core-skill-prototype/01-01-SUMMARY.md (Phase 1 patterns: directive-second-person voice; hypothesis-not-diagnosis framing; do not re-litigate)
    - philosophical-midwifery/SKILL.md (session loop steps 1–8 — NOT edited; SESSION_SCHEMA.md is a sibling, must not contradict the loop)
    - philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md (the 4 archetype machine names reused verbatim in belief_graph_template.json per D-07; presentation-protocol voice analog; in-place term-definition pattern)
    - philosophical-midwifery/references/QUESTION_TAXONOMY.md (voice/structure analog — neutral spec prose + directive guidance split; SECTION-ORDERING freedom noted in Phase 1 the-agent's-Discretion)
    - philosophical-midwifery/references/SAFETY_BOUNDARIES.md (sibling-voice analog; epistemic-humility tone reference)
  </read_first>
  <action>
    Author three additive artifacts under `philosophical-midwifery/`. Create the `assets/` directory (it does not yet exist). Do NOT edit any Phase 1 file (D-08 — Phase 2 is purely additive). Match Phase 1's established voice (D-09/D-10): directive second-person imperative where the file gives the agent guidance; neutral spec prose where it defines fields/types/enums.

    **(1) `philosophical-midwifery/references/SESSION_SCHEMA.md` (MTH-04, per A-1/A-2):** This file is the SINGLE SOURCE OF TRUTH for the session-JSON shape — the validator (Task 2) encodes its rules directly; there is no separate JSON-Schema sidecar (A-2). Open with a one-paragraph purpose statement tying the schema to design-draft §14 (the examined proposition is the atomic unit) and to Phase 1's inquiry loop (do not contradict SKILL.md steps 1–8). Then document EVERY field from `docs/design-draft.md` §9 as a field table or per-field blocks, each entry giving: field name, JSON type, required-or-optional, and a one-line description. The required field set is: `session_id` (string, uuid-format), `created_at` (string, ISO-8601 date-time), `phase` (string enum), `presenting_disturbance` (string), `concrete_example` (string), `candidate_beliefs` (array of objects), `selected_belief` (object), `normalized_proposition` (string), `definitions` (object), `implications` (array), `counterexamples` (array), `contradictions` (array), `revised_proposition` (string), `unresolved_tensions` (array), `next_questions` (array), `session_summary` (string). For `candidate_beliefs[]` objects document the `id` (string, `belief_N` convention per A-4), `text` (string), `confidence` (string enum low|medium|high), and `evidence` (array of strings) sub-fields. For `selected_belief` document `id` and `text`. Document the canonical 5-value `phase` enum per A-1: `intake`, `extraction`, `normalization`, `examination`, `synthesis`. Add an explicit note (A-1) that contradiction/revision and session-summary are sub-activities of `synthesis`, NOT separate phase values — this resolves the §7-says-6-vs-§9-says-5 tension by grounding the enum in the schema section. Document the `session_summary` field per A-3: it holds the rendered markdown string produced by filling in `assets/session_summary_template.md`; the validator treats it as a required non-empty string and does NOT parse markdown structure. Add a short "Worked example" subsection showing the recognition-determines-worth thread mapped onto the field names (belief text, normalized proposition "The value of work depends on recognition", revised proposition "Recognition affects visibility and opportunity, but does not determine intrinsic worth."), so the schema and the Task 2 fixtures/graph stay coherent. Close with a directive guidance block (second-person) telling the agent how to USE the schema during a session (populate fields as the inquiry proceeds; treat unfilled optional fields as empty, not fabricated).

    **(2) `philosophical-midwifery/assets/session_summary_template.md` (ART-01):** This is the FINAL inquiry artifact — the render target whose filled-in markdown string populates the schema's `session_summary` field (A-3). Source the section headings from `docs/design-draft.md` §7 Phase 6 AND the ART-01 requirement definition (presenting disturbance, concrete example, candidate pathologos, examined proposition, definitions, implications tested, counterexamples found, contradiction or weakness, revised proposition, unresolved tension, next inquiry target). Use a `## Session Summary` H2 heading followed by labeled fields (bolded labels, placeholder content like a single `…` or a short italic note like `*(fill from session)*`). Include ONE short filled-in example block (clearly marked as an example) using the recognition-determines-worth thread, so a maintainer can see the intended granularity. CRITICAL voice constraint: the template MUST NOT overclaim certainty — phrase the examined proposition, candidate pathologos, and revised proposition as outcomes of THIS conversation, not as established truths about the user. Echo Phase 1's epistemic-humility stance (hypotheses not diagnoses; the user is the authority on their own experience). Add a one-line usage note at the top stating that the rendered string populates `session_summary` (A-3) and that the validator does not parse markdown.

    **(3) `philosophical-midwifery/assets/belief_graph_template.json` (ART-02, per D-05/D-06/D-07/D-08):** A property-graph seed. Open the JSON with the optionality header markers (D-08): `"_v1_optional": true` and a `"_note"` string field explaining this is a future-compatible seed, NOT a required v1 step, and that Phase 1's SKILL.md session loop is left unchanged. Then provide the skeleton schema (documenting the shapes) and ONE worked example (D-05). Use the data model from D-06: a top-level object with `nodes` (array) and `edges` (array); each node is `{id, type, text, props}` and each edge is `{source, target, type}`. Document the allowed `type` values for nodes (at minimum `belief`, `proposition`, `contradiction`, `revised_proposition` — mapping to the stores in design-draft §8) and the typed edges `normalizes_to`, `contradicts`, `revises` (D-06). Belief nodes carry an OPTIONAL `pathologos_pattern` field in their `props` referencing the 4 Phase 1 archetype machine names (D-07): `conditional_worth`, `control_responsibility`, `belonging_rejection`, `perfection_flawlessness` (use these exact lowercase snake_case identifiers, matching Phase 1 PATHOLOGOS_PATTERNS.md). The single worked example traces the recognition-determines-worth thread (CONTEXT `<specifics>`): a belief node ("Recognition determines worth", pathologos_pattern `conditional_worth`) —`normalizes_to`→ a proposition node ("The value of work depends on recognition") —`contradicts`→ a contradiction node —`revises`→ a revised_proposition node ("Recognition affects visibility and opportunity, but does not determine intrinsic worth."). Keep the JSON valid (parseable by `json.load`). Do NOT implement recurrence machinery (first_seen_session, provenance, counters) — that is deferred to GRPH-01..03 (v2); the property-graph shape merely leaves room for it.
  </action>
  <verify>
    <automated>
      # SESSION_SCHEMA.md covers all 15 §9 fields + 5-value enum + A-1/A-2/A-3 notes
      python3 - <<'PY'
      import re,sys
      p="philosophical-midwifery/references/SESSION_SCHEMA.md"
      t=open(p).read()
      fields=["session_id","created_at","phase","presenting_disturbance","concrete_example","candidate_beliefs","selected_belief","normalized_proposition","definitions","implications","counterexamples","contradictions","revised_proposition","unresolved_tensions","next_questions","session_summary"]
      missing=[f for f in fields if f not in t]
      enum_ok=all(v in t for v in ["intake","extraction","normalization","examination","synthesis"])
      a1_ok="synthesis" in t and ("contradiction" in t.lower() or "revision" in t.lower())
      assert not missing, f"SESSION_SCHEMA.md missing fields: {missing}"
      assert enum_ok, "SESSION_SCHEMA.md missing phase enum values"
      assert a1_ok, "SESSION_SCHEMA.md missing A-1 synthesis sub-activity note"
      print("SESSION_SCHEMA.md OK")
      # summary template covers ART-01 content set + epistemic-humility marker
      s=open("philosophical-midwifery/assets/session_summary_template.md").read()
      arts=["presenting disturbance","concrete example","examined proposition","revised proposition","unresolved tension","next inquiry target"]
      sam=[a for a in arts if a.lower() not in s.lower()]
      assert not sam, f"summary template missing ART-01 sections: {sam}"
      assert "session_summary" in s, "summary template missing A-3 session_summary linkage note"
      print("session_summary_template.md OK")
      # belief graph: valid JSON + header markers + worked example + D-07 archetypes + D-06 edge types
      import json
      g=json.load(open("philosophical-midwifery/assets/belief_graph_template.json"))
      assert g.get("_v1_optional") is True, "missing _v1_optional:true (D-08)"
      assert "_note" in g and isinstance(g["_note"],str) and g["_note"], "missing _note (D-08)"
      assert "nodes" in g and "edges" in g, "missing nodes/edges (D-06)"
      gt=json.dumps(g)
      for e in ["normalizes_to","contradicts","revises"]: assert e in gt, f"missing edge type {e} (D-06)"
      for a in ["conditional_worth","control_responsibility","belonging_rejection","perfection_flawlessness"]: assert a in gt, f"missing archetype machine name {a} (D-07)"
      assert "recognition" in gt.lower(), "graph worked example not derived from recognition-determines-worth (D-05)"
      print("belief_graph_template.json OK")
      print("ALL TASK 1 CHECKS PASSED")
      PY
    </automated>
  </verify>
  <acceptance_criteria>
    - `philosophical-midwifery/assets/` directory exists and contains both `session_summary_template.md` and `belief_graph_template.json`.
    - `SESSION_SCHEMA.md` exists in `philosophical-midwifery/references/` alongside the three Phase 1 references, and documents ALL 15 fields from design-draft §9 plus the `candidate_beliefs[]`/`selected_belief` sub-fields, each with type + required/optional + description.
    - SESSION_SCHEMA.md lists the 5-value `phase` enum (`intake`, `extraction`, `normalization`, `examination`, `synthesis`) and contains an explicit note that contradiction/revision and session-summary are sub-activities of `synthesis` (A-1).
    - SESSION_SCHEMA.md documents that `session_summary` holds the rendered summary-template string and that the validator checks non-empty-string only, not markdown structure (A-3); it states it is the single source of truth (A-2).
    - `session_summary_template.md` includes all ART-01 sections (presenting disturbance, concrete example, candidate pathologos, examined proposition, definitions, implications, counterexamples, contradiction/weakness, revised proposition, unresolved tension, next inquiry target) and does NOT assert certainty beyond the conversation (epistemic-humility voice).
    - `belief_graph_template.json` is valid JSON (parses with `json.load`), opens with `"_v1_optional": true` + a non-empty `_note` (D-08), uses the D-06 node/edge shapes, includes all three typed edge types (`normalizes_to`, `contradicts`, `revises`), references all 4 archetype machine names via optional `pathologos_pattern` (D-07), and contains ONE recognition-determines-worth worked example (D-05) — no recurrence machinery (deferred GRPH items absent).
    - No Phase 1 file is modified (`git diff --name-only HEAD` shows only the 3 new files after this task is staged).
  </acceptance_criteria>
  <done>
    Three additive artifacts authored, cross-coherent on the recognition-determines-worth example, voice-matched to Phase 1, all automated checks above print "ALL TASK 1 CHECKS PASSED", and `git status` shows Phase 1 files untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the machine-checkable loop — validate_session_schema.py + valid.json + invalid.json (TDD: fixtures encode the contract first, then the validator satisfies them)</name>
  <files>
    philosophical-midwifery/scripts/validate_session_schema.py
    philosophical-midwifery/scripts/fixtures/valid.json
    philosophical-midwifery/scripts/fixtures/invalid.json
  </files>
  <read_first>
    - philosophical-midwifery/references/SESSION_SCHEMA.md (authored in Task 1 — the enum and field set the validator MUST match; A-2 single source of truth)
    - .planning/phases/02-structured-session-artifacts/02-CONTEXT.md (D-01 strictness, D-02 dual output, D-03 collect-all, D-04 fixtures + >=2 error classes, A-4 CLI shape + ISO-8601 + belief_N convention)
    - docs/design-draft.md (§9 State Schema — the canonical field shapes and the worked example; §7 Phase 5 contradiction example for fixture content)
    - .planning/phases/01-core-skill-prototype/01-CONTEXT.md (D-12 shared recognition-determines-worth example reused for fixtures)
  </read_first>
  <behavior>
    The validator's contract, encoded by the two fixtures BEFORE the script is written:
    - `valid.json` is a complete session JSON (all required fields present, correct types, valid `phase` value, ISO-8601 `created_at`, `belief_N` ids, non-empty `session_summary`) derived from the recognition-determines-worth thread.
    - `invalid.json` is a deliberately-broken sibling of `valid.json` that seeds AT LEAST TWO distinct error classes: (a) a missing required field, and (b) a bad `phase` value not in the enum. It may seed more.
    - `python3 scripts/validate_session_schema.py scripts/fixtures/valid.json` exits 0 and prints no errors (valid:true in --json).
    - `python3 scripts/validate_session_schema.py scripts/fixtures/invalid.json` exits 1, reports BOTH error classes in one pass (collect-all, D-03), one human-readable line per error in default mode.
    - `python3 scripts/validate_session_schema.py scripts/fixtures/invalid.json --json` exits 1 and prints `{"valid": false, "errors": [...]}` where the errors array names the missing field AND the bad phase value.
    - A non-ISO-8601 `created_at` is reported as an error.
    - A wrong-typed field (e.g., `candidate_beliefs` as a string) is reported as an error.
  </behavior>
  <action>
    Create the `philosophical-midwifery/scripts/` directory and the `philosophical-midwifery/scripts/fixtures/` subdirectory. Write the two fixtures FIRST (RED — they define the contract), then the validator (GREEN — it satisfies them). Run `python3` at `/opt/homebrew/bin/python3` (Python 3.14; no pyproject.toml/requirements.txt exists). **D-01: the validator MUST be stdlib-only** — only `sys`, `json`, `datetime`, `os`, `argparse`/manual argv parsing are permitted. DO NOT import or add `jsonschema`, `pyyaml`, or any third-party package. DO NOT use `subprocess`, `eval`, `exec`, `os.system`, or any network call (threat T-02-01 mitigation).

    **(RED) Fixtures** (both derived from the recognition-determines-worth thread per D-04; cross-coherent with the Task 1 graph example and SESSION_SCHEMA.md worked example):
    - `scripts/fixtures/valid.json`: a COMPLETE, schema-valid session JSON. Populate every required field with recognition-determines-worth content: `phase` = `"synthesis"`, `created_at` = a valid ISO-8601 date-time (e.g., `"2026-06-24T12:00:00Z"`), `candidate_beliefs` = an array with one object using `id:"belief_1"` (A-4 convention), `text:"Recognition determines worth."`, `confidence:"medium"`, `evidence:[...]`; `selected_belief` = `{id:"belief_1", text:"Recognition determines worth."}`; `normalized_proposition` = `"The value of work depends on recognition."`; `revised_proposition` = `"Recognition affects visibility and opportunity, but does not determine intrinsic worth."`; fill `definitions`, `implications`, `counterexamples`, `contradictions`, `unresolved_tensions`, `next_questions` with short recognition-themed strings; `session_summary` = a non-empty markdown string (may be the filled-in summary-template content). This fixture MUST validate clean (exit 0).
    - `scripts/fixtures/invalid.json`: a sibling of `valid.json` with AT LEAST two deliberate breaks (D-04): (a) REMOVE a required field entirely (e.g., delete `normalized_proposition`) to exercise the required-field check; (b) set `phase` to a value NOT in the enum (e.g., `"revision"`) to exercise the enum check. Optionally seed a third break (e.g., a malformed `created_at` or a wrong-typed field) to further demonstrate type/date checks. Keep the rest of the content recognition-themed so the fixture is recognizably a broken sibling of `valid.json`.

    **(GREEN) Validator** `scripts/validate_session_schema.py` (per D-01/D-02/D-03/A-4):
    - CLI shape (A-4): `validate_session_schema.py <path> [--json]`. Use `argparse` or manual argv parsing. Default mode prints CLI prose (one human-readable line per error); `--json` mode prints a single JSON document `{"valid": bool, "errors": [...]}`. Exit code: 0 if valid, 1 if invalid (both modes).
    - Load the JSON file at `<path>`; if the file is missing or not valid JSON, report a single top-level error (and exit 1).
    - Encode the rules DIRECTLY from SESSION_SCHEMA.md (A-2 — no sidecar file). Required + type + enum strictness (D-01):
      - REQUIRED: every required key (the 15 fields listed in Task 1) exists and is non-empty (empty string / empty array / empty object counts as a missing value for scalar/array fields where emptiness is meaningless — at minimum, a missing key is an error; for strings, an empty string is an error).
      - TYPE: each field's value type matches the schema (`session_id`/`created_at`/`phase`/`presenting_disturbance`/`concrete_example`/`normalized_proposition`/`revised_proposition`/`session_summary` = str; `candidate_beliefs` = list; `selected_belief` = dict; `definitions` = dict; `implications`/`counterexamples`/`contradictions`/`unresolved_tensions`/`next_questions` = list). `candidate_beliefs[]` entries are dicts with `id`/`text`/`confidence`/`evidence`. `selected_belief` has `id`/`text`.
      - ENUM: `phase` is one of `intake`, `extraction`, `normalization`, `examination`, `synthesis` (the SAME 5 values documented in SESSION_SCHEMA.md per A-1 — keep them in sync).
      - DATE (A-4): `created_at` parses as ISO-8601 (use `datetime.datetime.fromisoformat`, accepting a trailing `Z` by normalizing it to `+00:00`; a parse failure is an error).
      - `session_summary` is treated as a required non-empty string ONLY (A-3) — do NOT parse markdown structure.
    - ERROR COLLECTION (D-03): collect ALL errors in one pass before reporting. Do not stop at the first error. Each error entry should name the field path and the problem (e.g., `"phase: invalid value 'revision' (expected one of intake, extraction, normalization, examination, synthesis)"`, `"normalized_proposition: missing required field"`).
    - Keep the script readable and well-structured (a `validate(data) -> list[str]` function + a `main()` for argv/IO separation). Add a module docstring naming the requirement (ART-03) and the source of truth (SESSION_SCHEMA.md, A-2).

    After writing all three files, RUN the validator against BOTH fixtures and confirm the behavior block holds (valid → exit 0 + no errors; invalid → exit 1 + both error classes + correct --json shape). Iterate on the validator until GREEN.
  </action>
  <verify>
    <automated>
      # Behavior contract: run validator against both fixtures, assert exit codes + error content + --json shape
      python3 - <<'PY'
      import json, subprocess, sys
      V="philosophical-midwifery/scripts/validate_session_schema.py"
      PY=sys.executable
      def run(fix, json_flag=False):
        cmd=[PY,V,f"philosophical-midwifery/scripts/fixtures/{fix}.json"]
        if json_flag: cmd.append("--json")
        r=subprocess.run(cmd,capture_output=True,text=True)
        return r.returncode,r.stdout,r.stderr
      # valid -> exit 0
      rc,out,err=run("valid")
      assert rc==0, f"valid.json should exit 0, got {rc}; stderr={err}"
      # valid --json -> valid:true
      rc,out,err=run("valid",True)
      assert rc==0, f"valid --json exit {rc}"
      j=json.loads(out); assert j["valid"] is True and j["errors"]==[], f"bad valid --json payload: {j}"
      # invalid -> exit 1
      rc,out,err=run("invalid")
      assert rc==1, f"invalid.json should exit 1, got {rc}"
      # invalid --json -> valid:false + >=2 error classes incl missing-field + bad-phase
      rc,out,err=run("invalid",True)
      assert rc==1, f"invalid --json exit {rc}"
      j=json.loads(out); assert j["valid"] is False, f"invalid --json valid must be false: {j}"
      errs=" ".join(j["errors"]).lower()
      has_missing = ("missing" in errs) or ("required" in errs)
      has_phase = ("phase" in errs)
      assert has_missing and has_phase, f"invalid.json did not seed both error classes; errors={j['errors']}"
      assert len(j["errors"])>=2, f"collect-all (D-03) must report >=2 errors; got {j['errors']}"
      # stdlib-only gate (D-01 / T-02-01 mitigation)
      src=open(V).read()
      for bad in ["import jsonschema","import yaml","import requests","subprocess","os.system"," eval(","exec("]:
        assert bad not in src, f"validator must be stdlib-only and sandbox-safe (D-01); found '{bad}'"
      print("ALL TASK 2 BEHAVIOR CHECKS PASSED")
      PY
    </automated>
  </verify>
  <acceptance_criteria>
    - `philosophical-midwifery/scripts/` and `philosophical-midwifery/scripts/fixtures/` directories exist with `validate_session_schema.py`, `valid.json`, `invalid.json`.
    - `python3 philosophical-midwifery/scripts/validate_session_schema.py philosophical-midwifery/scripts/fixtures/valid.json` exits 0 and reports no errors.
    - `python3 ... invalid.json` (no flag) exits 1 and prints one human-readable line per error, including BOTH a missing-required-field error AND a bad-`phase`-value error (collect-all, D-03).
    - `python3 ... invalid.json --json` exits 1 and prints valid JSON `{"valid": false, "errors": [...]}` whose errors array contains >=2 entries spanning the missing-field and bad-phase classes (D-02/D-04).
    - `python3 ... valid.json --json` exits 0 and prints `{"valid": true, "errors": []}`.
    - The `phase` enum list inside the validator source is byte-identical to the 5 values documented in SESSION_SCHEMA.md (`intake`, `extraction`, `normalization`, `examination`, `synthesis`) — A-1/A-2 in sync.
    - The validator imports ONLY stdlib modules (no `jsonschema`/`pyyaml`/`requests`); uses no `subprocess`/`eval`/`exec`/`os.system`/network (D-01 + T-02-01 mitigation).
    - Both fixtures derive from the recognition-determines-worth thread (D-04); `invalid.json` is recognizably a broken sibling of `valid.json`.
    - No Phase 1 file is modified.
  </acceptance_criteria>
  <done>
    Validator passes `valid.json` (exit 0), fails `invalid.json` (exit 1) with collect-all reporting of >=2 distinct error classes in both prose and --json modes, is stdlib-only and sandbox-safe, its enum matches SESSION_SCHEMA.md exactly, and both fixtures are recognition-determines-worth-derived.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human review — summary-template voice, belief-graph optionality signaling, and cross-file coherence</name>
  <files>
    # Read-only review of the Phase 2 artifacts (no file modifications in this task)
    philosophical-midwifery/references/SESSION_SCHEMA.md
    philosophical-midwifery/assets/session_summary_template.md
    philosophical-midwifery/assets/belief_graph_template.json
    philosophical-midwifery/scripts/validate_session_schema.py
    philosophical-midwifery/scripts/fixtures/valid.json
    philosophical-midwifery/scripts/fixtures/invalid.json
  </files>
  <read_first>
    - philosophical-midwifery/references/SESSION_SCHEMA.md
    - philosophical-midwifery/assets/session_summary_template.md
    - philosophical-midwifery/assets/belief_graph_template.json
    - philosophical-midwifery/scripts/validate_session_schema.py
    - philosophical-midwifery/scripts/fixtures/valid.json
    - philosophical-midwifery/scripts/fixtures/invalid.json
    - philosophical-midwifery/SKILL.md (the unchanged Phase 1 loop — verify SESSION_SCHEMA.md does not contradict it)
    - philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md (verify the 4 archetype machine names match between graph and this reference)
    - .planning/PROJECT.md (Constraints + Key Decisions — epistemic humility, examined-proposition atomic unit)
  </read_first>
  <action>
    Perform a human review of the six Phase 2 artifacts across six checks (detailed in `<how-to-verify>` below): (1) summary-template epistemic-humility voice; (2) belief-graph optionality signaling (`_v1_optional`/`_note`, D-08); (3) recognition-determines-worth cross-file coherence (schema + graph + fixtures, D-04/D-05/D-12); (4) schema ↔ Phase 1 SKILL.md non-contradiction (D-08); (5) pathologos machine-name consistency (D-07); (6) optional CLI spot-run (A-4). The validator's mechanical behavior is already asserted by the automated checks in Tasks 1–2; this checkpoint judges what automation CANNOT: tone, optionality framing, and cross-file coherence. This mirrors Phase 1's blocking human-verify gate on safety-prose voice and cross-file consistency. No file modifications occur in this task; on failure, describe the issue and route back to the relevant authoring task.
  </action>
  <what-built>
    The Phase 2 artifact layer (Task 1: SESSION_SCHEMA.md, session_summary_template.md, belief_graph_template.json) and the machine-checkable loop (Task 2: validate_session_schema.py + valid.json + invalid.json).
  </what-built>
  <how-to-verify>
    1. **Summary-template voice (ART-01, PROJECT epistemic-humility constraint):** Read `session_summary_template.md`. Confirm the examined proposition, candidate pathologos, and revised proposition are phrased as outcomes of THIS conversation, not as established truths about the user. Flag any line that reads as a diagnosis, a certainty claim, or reassurance. (Compare against Phase 1 PATHOLOGOS_PATTERNS.md's "hypothesis, never a diagnosis" stance — the template should match that register.)
    2. **Belief-graph optionality signaling (D-08):** Read `belief_graph_template.json`. Confirm the `_v1_optional: true` header and `_note` clearly communicate that graph population is NOT a required v1 step and that Phase 1's SKILL.md loop is unchanged. Confirm nothing in the file implies the agent MUST populate the graph during a v1 session.
    3. **Cross-file coherence (D-04/D-05/D-12):** Confirm the recognition-determines-worth thread is consistent across `SESSION_SCHEMA.md` (worked example), `belief_graph_template.json` (worked example: belief → proposition → contradiction → revised_proposition), and `scripts/fixtures/{valid,invalid}.json`. The normalized proposition ("The value of work depends on recognition") and revised proposition ("Recognition affects visibility and opportunity, but does not determine intrinsic worth") should match across all of them.
    4. **Schema ↔ SKILL.md non-contradiction (D-08):** Confirm `SESSION_SCHEMA.md`'s phase descriptions and field flow align with (do not contradict) the 8-step inquiry loop in Phase 1's `SKILL.md`.
    5. **Pathologos machine-name consistency (D-07):** Confirm the 4 archetype identifiers in `belief_graph_template.json` (`conditional_worth`, `control_responsibility`, `belonging_rejection`, `perfection_flawlessness`) match the 4 archetypes named in Phase 1 `PATHOLOGOS_PATTERNS.md`.
    6. (Optional) Spot-run: `python3 philosophical-midwifery/scripts/validate_session_schema.py philosophical-midwifery/scripts/fixtures/valid.json` (expect exit 0) and `... invalid.json --json` (expect exit 1 + the errors array) to eyeball the CLI ergonomics (A-4).
  </how-to-verify>
  <verify>
    <human-check>
      Reviewer confirms all six checks pass (or flags specific failures). This is a blocking human gate — no automated assertion can substitute for the voice/optionality/coherence judgment.
    </human-check>
  </verify>
  <done>
    Reviewer types "approved" confirming: (a) summary template reads with epistemic humility (no certainty/diagnosis/reassurance); (b) belief graph clearly signals v1-optionality and leaves Phase 1 SKILL.md unchanged; (c) recognition-determines-worth thread is consistent across schema, graph, and both fixtures; (d) SESSION_SCHEMA.md does not contradict Phase 1 SKILL.md's loop; (e) the 4 pathologos machine names match between the graph and PATHOLOGOS_PATTERNS.md. On approval, the executor writes the Phase 2 summary.
  </done>
  <resume-signal>
    Type "approved" if all six checks pass, or describe which check failed and what to change. On approval, the executor writes the Phase 2 summary and the phase is complete.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → validator script | A human runs `python3 validate_session_schema.py <path>`; the `<path>` arg selects which local file is read. |
| fixture file → validator | The validator ingests arbitrary JSON from disk at a user-supplied path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | `validate_session_schema.py` is a new code-execution surface in the package | mitigate | D-01 stdlib-only constraint; task action forbids `subprocess`/`eval`/`exec`/`os.system`/network; the script reads exactly one file path and prints field names + error types only. Automated gate in Task 2 `<verify>` asserts none of these dangerous APIs appear in source. |
| T-02-02 | Information Disclosure | Validator output could echo fixture contents | accept | Output is limited to field paths + error descriptions; fixtures contain only synthetic recognition-determines-worth content (no PII, no secrets). Low value, low risk. |
| T-02-03 | Repudiation | n/a — no audit log in v1 | accept | Validator is a local dev tool; no multi-user audit requirement. Phase 3 (EVAL-03) adds package-completeness checks, not audit. |
| T-02-SC | Tampering (supply chain) | npm/pip/cargo package installs | accept (N/A) | D-01 mandates stdlib-only — there are NO package-manager installs in this phase. No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

## Artifacts this phase produces

Files (all under `philosophical-midwifery/`, all NEW — no Phase 1 file is edited):

- `philosophical-midwifery/references/SESSION_SCHEMA.md` — canonical session-state field definitions + 5-value `phase` enum (MTH-04, A-1/A-2).
- `philosophical-midwifery/assets/session_summary_template.md` — final inquiry artifact template; render target for `session_summary` (ART-01, A-3).
- `philosophical-midwifery/assets/belief_graph_template.json` — property-graph seed + ONE recognition-determines-worth worked example; optional for v1 (ART-02, D-05/D-06/D-07/D-08).
- `philosophical-midwifery/scripts/validate_session_schema.py` — stdlib-only validator; required + type + enum checks; collect-all reporting; dual output (ART-03, D-01/D-02/D-03).
- `philosophical-midwifery/scripts/fixtures/valid.json` — schema-valid recognition-determines-worth session (SC5, D-04).
- `philosophical-midwifery/scripts/fixtures/invalid.json` — invalid sibling seeding >=2 distinct error classes (SC5, D-04).

Symbols / identifiers this phase introduces (consumed by Phase 3 EVAL-02/03 and v2 HAR-*/GRPH-*):

- **CLI flag:** `--json` on `validate_session_schema.py` → emits `{"valid": bool, "errors": [...]}` (D-02, A-4).
- **`phase` enum values (canonical):** `intake`, `extraction`, `normalization`, `examination`, `synthesis` (A-1; documented in SESSION_SCHEMA.md AND encoded in the validator source — kept byte-identical).
- **Graph edge types:** `normalizes_to`, `contradicts`, `revises` (D-06).
- **Graph node types:** `belief`, `proposition`, `contradiction`, `revised_proposition` (D-06, mapping to design-draft §8 stores).
- **Belief-node optional field:** `pathologos_pattern` (D-07), referencing the 4 Phase 1 archetype machine names: `conditional_worth`, `control_responsibility`, `belonging_rejection`, `perfection_flawlessness`.
- **JSON header markers:** `_v1_optional` (boolean true) and `_note` (string) at the top of `belief_graph_template.json` (D-08 optionality signaling).
- **Belief id convention:** `belief_N` (A-4).
- **Date format:** ISO-8601 for `created_at` (A-4).

<verification>
**Automated (per task):**
- Task 1: python heredoc asserts all 15 §9 fields present in SESSION_SCHEMA.md, 5-value enum present, A-1/A-2/A-3 notes present, summary template covers ART-01 sections + A-3 linkage, belief graph is valid JSON with `_v1_optional`/`_note`/edge types/archetype names/recognition example.
- Task 2: python heredoc runs the validator against both fixtures, asserts exit codes (0 valid / 1 invalid), the --json payload shape (`{"valid":bool,"errors":[...]}`), collect-all behavior (>=2 errors spanning missing-field + bad-phase), and the stdlib-only/sandbox-safe source gate.
- Phase 1 untouched: `git diff --name-only HEAD~..HEAD` across the phase shows ONLY the 6 new files (asserted in each task's `git status` check).

**Human (Task 3 blocking checkpoint):** summary-template epistemic-humility voice; belief-graph optionality signaling; recognition-determines-worth cross-file coherence; schema↔SKILL.md non-contradiction; pathologos machine-name consistency.

**Cross-phase (Phase 3 will add, NOT this phase):** EVAL-02 behavioral/dialogue fixtures; EVAL-03 package-completeness check. The 6 files above land at the paths Phase 3 expects so its checks can find them.
</verification>

<success_criteria>
Mapped to ROADMAP §"Phase 2: Structured Session Artifacts" Success Criteria:

1. ✅ `SESSION_SCHEMA.md` defines all required session state fields and allowed phase values — Task 1 (MTH-04).
2. ✅ `assets/session_summary_template.md` captures the final inquiry artifact without asserting certainty beyond the conversation — Task 1 + Task 3 voice review (ART-01).
3. ✅ `assets/belief_graph_template.json` provides a future-compatible graph seed while remaining optional for v1 — Task 1 (`_v1_optional:true`) + Task 3 optionality review (ART-02).
4. ✅ `scripts/validate_session_schema.py` validates representative session JSON and reports missing/invalid fields clearly — Task 2 (ART-03, D-01/D-02/D-03).
5. ✅ At least one valid and one invalid fixture demonstrate validator behavior — Task 2 (SC5, D-04).

Plus cross-cutting: Phase 1 package untouched (D-08); recognition-determines-worth thread coherent across schema + graph + fixtures (D-04/D-05/D-12); all 4 requirement IDs {MTH-04, ART-01, ART-02, ART-03} covered.
</success_criteria>

<output>
Create `.planning/phases/02-structured-session-artifacts/02-01-SUMMARY.md` when done (after Task 3 approval). Use the GSD summary template; record the 6 files created, requirements completed (MTH-04, ART-01, ART-02, ART-03), the locked decisions implemented (D-01..D-08, A-1..A-4), and the recognition-determines-worth coherence as the key pattern.
</output>
