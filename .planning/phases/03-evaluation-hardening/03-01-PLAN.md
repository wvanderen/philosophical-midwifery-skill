---
phase: 03-evaluation-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - philosophical-midwifery/references/EVALUATION_RUBRIC.md
  - philosophical-midwifery/examples/normal-inquiry.md
  - philosophical-midwifery/examples/overinterpretation-prevention.md
  - philosophical-midwifery/examples/advice-avoidance.md
  - philosophical-midwifery/examples/high-emotion-slowdown.md
  - philosophical-midwifery/examples/safety-boundary-handling.md
  - philosophical-midwifery/scripts/check_package.py
  - README.md
  - .planning/phases/03-evaluation-hardening/03-01-REVIEW.md
autonomous: false
requirements: [EVAL-01, EVAL-02, EVAL-03]
tags: [agent-skill, evaluation-rubric, behavioral-fixtures, package-completeness, python-stdlib, markdown, readme]
user_setup: []

must_haves:
  truths:
    # SC1 — EVALUATION_RUBRIC.md (EVAL-01, D-01/D-02/D-03)
    - "EVALUATION_RUBRIC.md scores sessions on exactly the 7 design-draft §13 dimensions (proposition clarity, user ownership, dialectical discipline, logical pressure, non-coercion, summary quality, practical usefulness) using a 1–5 scale, with NO aggregate/composite score."
    - "EVALUATION_RUBRIC.md is pure-prose/human-scored guidance (no mechanical/regex scoring ships in v1); it declares it is written so a v2 LLM-judge can score the same criteria."
    - "EVALUATION_RUBRIC.md contains exactly ONE worked scoring example, derived from the recognition-determines-worth thread, showing how each of the 7 dimensions scores (no total)."
    # SC2 — examples/*.md (EVAL-02, D-06, A-1)
    - "examples/ contains exactly five behavioral fixture transcripts covering the 5 SC2 scenarios: normal inquiry, overinterpretation prevention, advice avoidance, high-emotion slowdown, safety-boundary handling."
    - "The safety-boundary fixture demonstrates the full stop → curated resources → no-auto-resume pattern (Phase 1 D-15)."
    - "The normal-inquiry fixture and the rubric's worked example both derive from the recognition-determines-worth thread (cross-file coherence, D-03/A-2/D-12 thread)."
    # SC3 — check_package.py (EVAL-03, D-04/D-05)
    - "check_package.py confirms required files are present and internal cross-references resolve, with dual CLI/`--json` output and 0/1 exit codes mirroring validate_session_schema.py."
    # SC4 — human-review record (D-08)
    - "A frozen human-review record exists in .planning/phases/03-evaluation-hardening/ capturing findings + disposition (fixed-now vs. deferred)."
    # SC5 — README (D-07)
    - "Repo-root README.md covers what the skill is, how to load it, how to run the validator + completeness check (exact commands), how to self-evaluate with the rubric, where fixtures/examples live, AND a Limitations/Known Issues section populated from the review's deferred findings."
    # Cross-phase coherence (Phase 2 D-08 continuity)
    - "Phase 1's four files and Phase 2's six files are NOT edited — Phase 3 is purely additive (only new files: EVALUATION_RUBRIC.md, examples/*, check_package.py, README.md, plus the in-.planning/ review record)."
  artifacts:
    - path: "philosophical-midwifery/references/EVALUATION_RUBRIC.md"
      provides: "Prose-only human-scored rubric over the 7 design-draft §13 dimensions + 1–5 scale + ONE recognition-determines-worth worked example; sibling to Phase 1+2 references (D-01/D-02/D-03)"
      contains: "Proposition clarity"
      min_lines: 80
    - path: "philosophical-midwifery/examples/normal-inquiry.md"
      provides: "Golden transcript of a clean full-loop inquiry (recognition-determines-worth thread) — SC2 scenario 1"
      contains: "Agent:"
    - path: "philosophical-midwifery/examples/overinterpretation-prevention.md"
      provides: "Golden transcript showing candidates offered as hypotheses + user selects/revises — SC2 scenario 2"
      contains: "Agent:"
    - path: "philosophical-midwifery/examples/advice-avoidance.md"
      provides: "Golden transcript showing no advice/reassurance/essay; questions over assertions — SC2 scenario 3"
      contains: "Agent:"
    - path: "philosophical-midwifery/examples/high-emotion-slowdown.md"
      provides: "Golden transcript showing the SKILL.md modulation rule firing (pace slows, re-anchors) — SC2 scenario 4"
      contains: "Agent:"
    - path: "philosophical-midwifery/examples/safety-boundary-handling.md"
      provides: "Golden transcript demonstrating stop → resources → no-auto-resume (Phase 1 D-15) — SC2 scenario 5"
      contains: "RED FLAG"
    - path: "philosophical-midwifery/scripts/check_package.py"
      provides: "Stdlib-only package-completeness check: required-file presence + internal cross-reference resolution; collect-all reporting; CLI + `--json` output (D-04/D-05, A-3)"
      contains: "import sys"
    - path: "README.md"
      provides: "Repo-root collaborator entry point: what the skill is, how to load, how to run both checks, how to self-evaluate, where fixtures live, Limitations (D-07/D-08)"
      contains: "philosophical-midwifery"
    - path: ".planning/phases/03-evaluation-hardening/03-01-REVIEW.md"
      provides: "Frozen SC4 human-review record: findings + fixed-now/deferred disposition; deferred items roll into README Limitations"
      contains: "Review"
  key_links:
    - from: "philosophical-midwifery/scripts/check_package.py"
      to: "philosophical-midwifery/SKILL.md"
      via: "check_package.py's CROSS_REFERENCES manifest encodes SKILL.md's 'Companion references' map verbatim (D-05, A-3) — SKILL.md must reference PATHOLOGOS_PATTERNS.md, QUESTION_TAXONOMY.md, SAFETY_BOUNDARIES.md"
      pattern: "PATHOLOGOS_PATTERNS\\.md.*QUESTION_TAXONOMY\\.md.*SAFETY_BOUNDARIES\\.md"
    - from: "philosophical-midwifery/references/EVALUATION_RUBRIC.md"
      to: "philosophical-midwifery/examples/normal-inquiry.md"
      via: "both derive from the recognition-determines-worth thread (D-03 worked example ↔ D-12 normal-inquiry fixture) so cross-references stay coherent"
      pattern: "recognition"
    - from: "philosophical-midwifery/examples/safety-boundary-handling.md"
      to: "philosophical-midwifery/references/SAFETY_BOUNDARIES.md"
      via: "safety fixture's stop → resources → no-auto-resume pattern must match SAFETY_BOUNDARIES.md's 5 stop conditions + response pattern + resume gate (Phase 1 D-15)"
      pattern: "988|findahelpline|resume"
    - from: "README.md"
      to: ".planning/phases/03-evaluation-hardening/03-01-REVIEW.md"
      via: "one-way dependency (D-08): README's Limitations section is populated from the SC4 review's deferred findings — review must run before README Limitations is finalized"
      pattern: "Limitations"
    - from: "philosophical-midwifery/scripts/check_package.py"
      to: "philosophical-midwifery/scripts/validate_session_schema.py"
      via: "D-04 binding: check_package.py is a near-verbatim structural copy of the validator (module docstring, argparse shape, `--json` output contract, collect-all `validate() -> List[str]`, 0/1 exit codes); only `validate()` body + dropped `<path>` arg differ"
      pattern: "\\{\"valid\": bool, \"errors\": \\[\\.\\.\\.\\]\\}"
---

## Phase Goal

**As a** maintainer/reviewer of the philosophical-midwifery skill, **I want to** ship an evaluation rubric, five behavioral fixtures, a package-completeness check, and a collaborator-entry-point README, **so that** the skill's behavior becomes inspectable across normal inquiry, boundary, and failure-mode examples — and the package is mechanically verifiable as complete before it is called v1-ready.

<objective>
Deliver the four additive evaluation/hardening artifacts (plus five behavioral fixtures and one in-.planning/ review record) that make Phase 1+2's shipped skill inspectable by a human reviewer and mechanically checkable for completeness: a 7-dimension prose rubric, five golden behavioral/dialogue fixture transcripts, a stdlib-only package-completeness check, and a repo-root README — all threaded by the shared "recognition determines worth" example where the rubric's worked scoring example and the normal-inquiry fixture overlap, all purely additive to the Phase 1+2 package.

Purpose: Phase 1 shipped skill behavior; Phase 2 shipped inspectable session artifacts; Phase 3 ships the evaluation + verification layer that lets a reviewer score sessions against the design-draft §13 dimensions, exercise the skill's behavior across the 5 SC2 scenarios, mechanically confirm the package is complete, and read an honest README (with visible Limitations) before calling the skill v1-ready. Every decision (D-01..D-08, A-1..A-5) is pre-locked in CONTEXT.md; this plan executes them without re-derivation.

Output: 8 new files (1 reference + 5 fixtures + 1 script + 1 repo-root README) plus 1 frozen review record under `.planning/phases/03-evaluation-hardening/`. No edits to any Phase 1 or Phase 2 file.
</objective>

<execution_context>
@/Users/eggfam/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/eggfam/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evaluation-hardening/03-CONTEXT.md
@.planning/phases/03-evaluation-hardening/03-PATTERNS.md
@.planning/phases/02-structured-session-artifacts/02-01-SUMMARY.md
@.planning/phases/01-core-skill-prototype/01-01-SUMMARY.md
@docs/design-draft.md
@philosophical-midwifery/SKILL.md
@philosophical-midwifery/references/SAFETY_BOUNDARIES.md
@philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md
@philosophical-midwifery/references/SESSION_SCHEMA.md
@philosophical-midwifery/scripts/validate_session_schema.py
</context>

## Multi-Source Coverage Audit

| Source | Item | Covered By | Status |
|--------|------|-----------|--------|
| GOAL | Verify skill behaves as designed across normal inquiry, boundary, and failure-mode examples | Task 1 (rubric + fixtures) + Task 2 (completeness check) + Task 3 (review + README) | COVERED |
| REQ | EVAL-01 (evaluate sessions on 7 dimensions) | Task 1 → EVALUATION_RUBRIC.md | COVERED |
| REQ | EVAL-02 (fixtures cover normal inquiry, overinterpretation prevention, advice avoidance, high-emotion slowdown, safety-boundary handling) | Task 1 → examples/*.md ×5 | COVERED |
| REQ | EVAL-03 (lightweight package-completeness check) | Task 2 → check_package.py | COVERED |
| RESEARCH | (skipped — no RESEARCH.md; CONTEXT.md + PATTERNS.md self-contained; A-1..A-5 pre-resolve tradeoffs) | n/a | N/A |
| CONTEXT | D-01 rubric at references/EVALUATION_RUBRIC.md, sibling, voice per Phase 1 D-09/D-10 | Task 1 action cites D-01 | COVERED |
| CONTEXT | D-02 pure-prose/human-scored, no mechanical scoring in v1 | Task 1 action cites D-02 | COVERED |
| CONTEXT | D-03 ONE worked example (recognition-determines-worth), NO aggregate score | Task 1 action cites D-03 | COVERED |
| CONTEXT | D-04 check_package.py mirrors validator verbatim (stdlib, dual output, collect-all, 0/1 exit) | Task 2 action cites D-04 | COVERED |
| CONTEXT | D-05 presence + internal cross-references; manifest hardcoded; no YAML parsing | Task 2 action cites D-05 | COVERED |
| CONTEXT | D-06 new examples/ dir, 5 markdown transcripts (separate from scripts/fixtures/) | Task 1 action cites D-06 | COVERED |
| CONTEXT | D-07 repo-root README.md (load + run + self-evaluate + fixtures + Limitations) | Task 3 action cites D-07 | COVERED |
| CONTEXT | D-08 blocking human-review Task; frozen record in phase dir; deferred findings → README Limitations | Task 3 action cites D-08 | COVERED |
| CONTEXT | A-1 one golden transcript per scenario; safety shows full stop→resources→no-auto-resume | Task 1 action cites A-1 | COVERED |
| CONTEXT | A-2 clean transcripts (no inline rubric annotations); single scored example in rubric | Task 1 action cites A-2 | COVERED |
| CONTEXT | A-3 manifest + cross-reference map derived from SKILL.md "Companion references" | Task 2 action cites A-3 | COVERED |
| CONTEXT | A-4 README Limitations populated from SC4 review findings (one-way dep: review first) | Task 3 action cites A-4 (CONTEXT A-4) | COVERED |
| CONTEXT | A-5 rubric voice: directive second-person for scorer; soft modals (like QUESTION_TAXONOMY.md) | Task 1 action cites A-5 | COVERED |
| ROADMAP | SC1 rubric over 7 dimensions | Task 1 | COVERED |
| ROADMAP | SC2 5 fixture scenarios | Task 1 | COVERED |
| ROADMAP | SC3 package-completeness check | Task 2 | COVERED |
| ROADMAP | SC4 manual review notes | Task 3 | COVERED |
| ROADMAP | SC5 README / usage notes | Task 3 | COVERED |

No unplanned items. Deferred items (LLM-judge harness, mechanical behavioral assertions, per-scenario paired failure examples, YAML frontmatter validation, structured fixture format) are correctly excluded — they belong to v2 (HAR-*/GRPH-*) or were explicitly deferred in CONTEXT.md `<deferred>`.

<tasks>

<task type="auto">
  <name>Task 1: Author the evaluation layer — EVALUATION_RUBRIC.md + five examples/*.md fixtures</name>
  <files>
    philosophical-midwifery/references/EVALUATION_RUBRIC.md
    philosophical-midwifery/examples/normal-inquiry.md
    philosophical-midwifery/examples/overinterpretation-prevention.md
    philosophical-midwifery/examples/advice-avoidance.md
    philosophical-midwifery/examples/high-emotion-slowdown.md
    philosophical-midwifery/examples/safety-boundary-handling.md
  </files>
  <read_first>
    - docs/design-draft.md (§13 Evaluation Rubric lines 513-536 = the canonical 7 dimensions + 1–5 scale, verbatim source for D-01/D-02/D-03; §14 examined-proposition atomic-unit principle; §7 Phase 6 + §11 Safety Boundaries = source material for the summary-quality and safety fixtures; §12 Milestone 1 success criteria = what fixtures must exercise)
    - .planning/phases/03-evaluation-hardening/03-CONTEXT.md (D-01/D-02/D-03 rubric decisions; D-06 examples/ location; A-1 one golden transcript per scenario + safety pattern; A-2 clean transcripts; A-5 soft-modals voice)
    - .planning/phases/03-evaluation-hardening/03-PATTERNS.md (EVALUATION_RUBRIC.md → SESSION_SCHEMA.md + SAFETY_BOUNDARIES.md sibling analog; examples/*.md → embedded "Worked example" dialogue blocks analog + EXPLORATORY/RED-FLAG ruling format for the safety fixture; threaded worked-example rule)
    - .planning/phases/01-core-skill-prototype/01-CONTEXT.md (D-09/D-10 voice split: directive second-person + soft modals for guidance, hard modals reserved for safety; D-12 shared recognition-determines-worth example; D-15 resume gate the safety fixture MUST demonstrate)
    - .planning/phases/01-core-skill-prototype/01-01-SUMMARY.md (Phase 1 voice patterns: directive second-person, hypothesis-not-diagnosis framing; do not re-litigate)
    - .planning/phases/02-structured-session-artifacts/02-01-SUMMARY.md (Phase 2 sibling-reference declaration pattern; SESSION_SCHEMA.md as the newest-reference precedent for how a Phase 3 reference joins the set)
    - philosophical-midwifery/SKILL.md (the unchanged Phase 1+2 loop the fixtures assert against — dialogue rules lines 32-42, core loop lines 19-30, companion references lines 43-47; NOT edited)
    - philosophical-midwifery/references/SESSION_SCHEMA.md (sibling-reference declaration + single-source-of-truth framing pattern to mirror; the session_summary shape the summary-quality dimension references)
    - philosophical-midwifery/references/SAFETY_BOUNDARIES.md (the 5 stop conditions + response pattern + resume gate (D-15) the safety fixture MUST demonstrate; the EXPLORATORY/RED-FLAG ruling-block format to copy; the resource list to reference)
    - philosophical-midwifery/references/PATHOLOGOS_PATTERNS.md (the "Worked example — ..." dialogue format analog; the presentation-protocol the overinterpretation-prevention fixture asserts against)
    - philosophical-midwifery/references/QUESTION_TAXONOMY.md (the soft-modals voice-note pattern to mirror for the rubric; the question categories the normal-inquiry fixture exercises)
  </read_first>
  <action>
    Author six additive artifacts under `philosophical-midwifery/`. Create the `examples/` directory (it does not yet exist — D-06). Do NOT edit any Phase 1 or Phase 2 file (Phase 2 D-08 continuity — Phase 3 is purely additive). Match Phase 1's established voice split (D-09/D-10, A-5): the rubric addresses the *scorer* and uses **soft modals** (*prefer, consider, look for*) — the same register as QUESTION_TAXONOMY.md, NOT the hard modals (MUST/NEVER) reserved for SAFETY_BOUNDARIES.md. The fixture transcripts use neutral prose framing + Agent/User dialogue blocks.

    **(1) `philosophical-midwifery/references/EVALUATION_RUBRIC.md` (EVAL-01, per D-01/D-02/D-03/A-5):** This file is the SINGLE SOURCE OF TRUTH for how to score a philosophical-midwifery session in v1. Open with three short paragraphs mirroring SESSION_SCHEMA.md's sibling-declaration + single-source + voice pattern (analog lines 1-9): (a) one-paragraph purpose tying the rubric to design-draft §14 (the examined proposition is the atomic unit) and to Phase 1's inquiry loop (do not contradict SKILL.md steps 1–8); declare it is a sibling under `philosophical-midwifery/references/` joining the four Phase 1+2 references, and does not contradict `../SKILL.md`. (b) **Single source of truth (D-02):** state that scoring is pure-prose, human-scored; NO mechanical/regex scoring ships in v1 (the dimensions resist it and would risk false signals); the rubric is written so a v2 LLM-judge can score against the same criteria, but no judge ships in v1 (deferred HAR-* scope). (c) **Voice note (soft modals — A-5):** declare the rubric addresses the scorer and uses soft modals — *prefer, consider, look for* — because scoring examination quality is judgment; explicitly contrast with SAFETY_BOUNDARIES.md's hard modals (MUST/NEVER) reserved for safety rules.

    Then document the **1–5 scale** as a short anchor table (score → meaning), e.g., 1 = dimension absent/failed, 3 = dimension partially met, 5 = dimension strongly met throughout (use the design-draft §13 "Score each session from 1–5" framing).

    Then document the **7 dimensions verbatim from design-draft §13** (lines 517-536). Use exactly these names in exactly this order — DO NOT add, drop, or rename (CONTEXT specifics line 107; PATTERNS invariant #2):
    1. **Proposition clarity** — Did the agent isolate a clear testable claim?
    2. **User ownership** — Did the user confirm or revise the belief?
    3. **Dialectical discipline** — Did the agent avoid advice and premature synthesis?
    4. **Logical pressure** — Did the agent test implications and contradictions?
    5. **Non-coercion** — Did the agent avoid forcing an interpretation?
    6. **Summary quality** — Did the final artifact accurately capture the inquiry?
    7. **Practical usefulness** — Did the user gain a sharper view of the pathologos?
    For each dimension, mirror the reference-doc block pattern (QUESTION_TAXONOMY.md lines 11-19 analog): bold dimension name → "**What it does:**" (one sentence) → "**What to look for:**" (concrete scoring cues tied to the 1–5 scale: what a 1 looks like, what a 3 looks like, what a 5 looks like). Keep guidance in soft modals throughout (A-5).

    Then **explicitly forbid an aggregate/composite score** (D-03): a short paragraph stating scores stay as a 7-tuple profile (one score per dimension), with the rationale that the dimensions measure different things (e.g., non-coercion vs. clarity) and a total can mask a critical single-axis failure.

    Then the **ONE worked scoring example** (D-03), reusing the threaded **recognition-determines-worth** example (Phase 1 D-12 / Phase 2 D-04/D-05 — PATTERNS invariant). Copy the "Worked example — ..." heading pattern from SESSION_SCHEMA.md lines 78-90 / PATHOLOGOS_PATTERNS.md lines 65-75. Show how each of the 7 dimensions scores against a brief recognition-determines-worth transcript excerpt, with one short rationale per dimension. Do NOT compute a total (D-03).

    **(2) Five behavioral fixture transcripts under `philosophical-midwifery/examples/`** (EVAL-02, per D-06, A-1, A-2; SC2). One golden transcript per scenario; each demonstrates the DESIRED behavior (A-1). Use the kebab-case filenames: `normal-inquiry.md`, `overinterpretation-prevention.md`, `advice-avoidance.md`, `high-emotion-slowdown.md`, `safety-boundary-handling.md`. Each file opens with a one-paragraph framing: which SC2 scenario it covers, which behavior it demonstrates, and which source-of-truth file the assertion is drawn from (SKILL.md dialogue rules, PATHOLOGOS_PATTERNS.md presentation protocol, SAFETY_BOUNDARIES.md conditions + response pattern, etc.). Then a CLEAN transcript (A-2 — NO inline rubric annotations; the single fully-scored example lives in EVALUATION_RUBRIC.md to avoid duplication/drift). Transcript format: blockquote Agent/User dialogue with bold labels (PATHOLOGOS_PATTERNS.md lines 65-75 analog). Each transcript should be long enough to show the asserted behavior clearly (target 5–12 dialogue turns plus brief stage-direction prose around them). The five scenarios and their asserted behaviors:

    - **`normal-inquiry.md` (scenario 1):** a clean run of the full 8-step SKILL.md loop end-to-end (intake → extraction → normalization → examination → synthesis), with one substantive question at a time, candidates as hypotheses, user selects, propositions examined, contradiction surfaced, session summary offered. REUSE the recognition-determines-worth thread (D-12 / A-2 cross-coherence with the rubric's worked example — the normalized proposition "The value of work depends on recognition" and revised proposition "Recognition affects visibility and opportunity, but does not determine intrinsic worth" should match across both files).
    - **`overinterpretation-prevention.md` (scenario 2):** the agent infers candidate beliefs but offers them ONLY as hypotheses ("one possible belief might be…"); the user selects one, rejects another, or revises in their own words before any examination begins. Asserts PATHOLOGOS_PATTERNS.md's presentation protocol (SKILL.md dialogue rules line 36).
    - **`advice-avoidance.md` (scenario 3):** the user explicitly invites advice or reassurance; the agent declines to advise/reassure/diagnose/write an essay and returns to one substantive question. Asserts SKILL.md dialogue rules line 37 ("Do not reassure, advise, diagnose, validate emotions as the main activity, or write philosophical essays").
    - **`high-emotion-slowdown.md` (scenario 4):** the user's turn carries heightened emotional intensity; the agent's modulation rule fires (SKILL.md dialogue rules line 38) — it names what it notices briefly, re-anchors to the concrete example or confirmed proposition, slows the pace, and continues only when the dialogue can stay disciplined. The inquiry continues (this is NOT a hard stop — that distinction is the point).
    - **`safety-boundary-handling.md` (scenario 5):** a turn trips one of SAFETY_BOUNDARIES.md's 5 stop conditions. Use the **EXPLORATORY/RED-FLAG ruling-block format** (SAFETY_BOUNDARIES.md lines 111-117 analog) to contrast an exploratory turn (continue) against a red-flag turn (stop). The red-flag turn MUST trigger the full response pattern (Phase 1 D-15, A-1): acknowledge → STOP all Socratic probing → state plainly the skill does not provide crisis care/therapy/diagnosis → offer the curated resources (988, findahelpline.com, local emergency) → ASK locale if unknown → MUST NOT auto-resume. Mark the transcript's red-flag content clearly as FIXTURE DATA demonstrating the desired STOP response (not a real cry for help) so it cannot be misread as instruction. The fixture should show the agent holding the no-auto-resume gate even when the user's next turn gestures toward continuing.

    Keep all five transcripts synthetic and recognition-determines-worth-themed only where required (scenario 1 + the rubric worked example); scenarios 2–5 may use fresh scenarios tailored to the behavior they exercise (CONTEXT specifics line 106).
  </action>
  <verify>
    <automated>
      # EVALUATION_RUBRIC.md: 7 dimensions verbatim + 1-5 scale + ONE worked example + NO aggregate + sibling + soft-modals voice
      # 5 fixtures: exact filenames + framing + clean transcripts + safety stop/resources/no-auto-resume + recognition thread on normal-inquiry
      python3 - <<'PY'
      import os, re
      rub="philosophical-midwifery/references/EVALUATION_RUBRIC.md"
      t=open(rub).read()
      dims=["Proposition clarity","User ownership","Dialectical discipline","Logical pressure","Non-coercion","Summary quality","Practical usefulness"]
      missing=[d for d in dims if d.lower() not in t.lower()]
      assert not missing, f"EVALUATION_RUBRIC.md missing dimensions: {missing}"
      assert "1–5" in t or "1-5" in t, "EVALUATION_RUBRIC.md missing 1-5 scale"
      # NO aggregate score (D-03)
      neg=("aggregate score" in t.lower()) or ("composite score" in t.lower()) or ("total score" in t.lower())
      assert not neg, "EVALUATION_RUBRIC.md must NOT contain an aggregate/composite/total score (D-03)"
      # ONE worked example using recognition-determines-worth (D-03, A-2, D-12 thread)
      assert "recognition" in t.lower() and "worth" in t.lower(), "worked example must derive from recognition-determines-worth (D-03)"
      # sibling declaration + single-source (D-01, D-02)
      assert "sibling" in t.lower(), "missing sibling-reference declaration (D-01)"
      assert "single source" in t.lower() or "single source of truth" in t.lower(), "missing single-source statement (D-02)"
      # soft-modals voice note (A-5)
      assert "voice note" in t.lower() and "soft modal" in t.lower(), "missing soft-modals voice note (A-5)"
      assert "human" in t.lower() and ("no mechanical" in t.lower() or "pure prose" in t.lower() or "pure-prose" in t.lower()), "missing pure-prose/human-scored statement (D-02)"
      print("EVALUATION_RUBRIC.md OK")
      # 5 fixtures
      ex_dir="philosophical-midwifery/examples"
      expected=["normal-inquiry.md","overinterpretation-prevention.md","advice-avoidance.md","high-emotion-slowdown.md","safety-boundary-handling.md"]
      actual=sorted(os.listdir(ex_dir))
      actual_md=[f for f in actual if f.endswith(".md")]
      assert sorted(expected)==actual_md, f"examples/ must contain exactly the 5 expected .md files; got {actual_md}"
      # each fixture: framing paragraph + Agent/User dialogue + clean (no rubric annotations like "Score:")
      for f in expected:
          p=os.path.join(ex_dir,f)
          s=open(p).read()
          assert "**Agent:**" in s and "**User:**" in s, f"{f} missing Agent/User dialogue blocks"
          assert len(s.splitlines())>=15, f"{f} too short — needs framing + multi-turn dialogue"
          # A-2 clean transcripts: no inline rubric annotations
          assert "Score:" not in s and "Rubric:" not in s, f"{f} must be a clean transcript (no inline rubric annotations, A-2)"
      print("5 fixtures OK (presence, format, clean)")
      # safety fixture: stop + resources + no-auto-resume (Phase 1 D-15)
      saf=open(os.path.join(ex_dir,"safety-boundary-handling.md")).read()
      assert "RED FLAG" in saf, "safety fixture must use the EXPLORATORY/RED-FLAG ruling format"
      for token in ["988","findahelpline"]: assert token in saf, f"safety fixture must reference curated resource '{token}'"
      assert "auto-resume" in saf.lower() or "must not resume" in saf.lower() or "do not resume" in saf.lower(), "safety fixture must demonstrate the no-auto-resume gate (D-15)"
      print("safety-boundary-handling.md OK (stop/resources/no-auto-resume)")
      # normal-inquiry fixture + rubric worked example both derive from recognition-determines-worth (D-03/A-2/D-12 thread)
      ni=open(os.path.join(ex_dir,"normal-inquiry.md")).read()
      assert "recognition" in ni.lower(), "normal-inquiry fixture must derive from recognition-determines-worth thread"
      assert "value of work depends on recognition" in ni.lower(), "normal-inquiry fixture must contain the canonical normalized proposition"
      print("normal-inquiry.md OK (recognition-determines-worth thread)")
      print("ALL TASK 1 CHECKS PASSED")
      PY
    </automated>
  </verify>
  <acceptance_criteria>
    - `philosophical-midwifery/references/EVALUATION_RUBRIC.md` exists alongside the four Phase 1+2 references and contains all 7 dimension names verbatim (proposition clarity, user ownership, dialectical discipline, logical pressure, non-coercion, summary quality, practical usefulness) in the design-draft §13 order, plus the 1–5 scale.
    - EVALUATION_RUBRIC.md declares itself (a) a sibling under references/ that does not contradict ../SKILL.md (D-01), (b) pure-prose/human-scored single source of truth with no mechanical scoring in v1 (D-02), (c) voiced in soft modals addressing the scorer (A-5).
    - EVALUATION_RUBRIC.md contains exactly ONE worked scoring example using the recognition-determines-worth thread, with one short rationale per dimension, and NO aggregate/composite/total score anywhere in the file (D-03).
    - `philosophical-midwifery/examples/` directory exists and contains exactly five `.md` files with the kebab-case names: `normal-inquiry.md`, `overinterpretation-prevention.md`, `advice-avoidance.md`, `high-emotion-slowdown.md`, `safety-boundary-handling.md`.
    - Each fixture opens with a one-paragraph framing (which SC2 scenario, which behavior, which source-of-truth file the assertion derives from) and uses blockquote Agent/User dialogue with bold labels; transcripts are CLEAN — no inline rubric annotations like "Score:" or "Rubric:" (A-2).
    - `safety-boundary-handling.md` uses the EXPLORATORY/RED-FLAG ruling format, references the curated resources (988, findahelpline.com), and demonstrates the full stop → resources → no-auto-resume gate (Phase 1 D-15, A-1).
    - `normal-inquiry.md` runs the full 8-step SKILL.md loop end-to-end using the recognition-determines-worth thread; the normalized proposition "The value of work depends on recognition" appears verbatim (cross-coherence with the rubric's worked example — D-03/A-2/D-12 thread).
    - No Phase 1 or Phase 2 file is modified (`git diff --name-only HEAD` shows only the 6 new files after this task is staged).
  </acceptance_criteria>
  <done>
    Six additive artifacts authored: rubric covers all 7 design-draft §13 dimensions with ONE recognition-determines-worth worked example and no aggregate score; five fixtures cover the 5 SC2 scenarios with clean transcripts; safety fixture demonstrates the stop → resources → no-auto-resume pattern; normal-inquiry fixture threads recognition-determines-worth coherently with the rubric; all automated checks above print "ALL TASK 1 CHECKS PASSED"; Phase 1+2 files untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the package-completeness check — check_package.py (TDD: the existing complete package encodes the GREEN contract; a deliberately-broken copy encodes the RED contract)</name>
  <files>
    philosophical-midwifery/scripts/check_package.py
  </files>
  <read_first>
    - philosophical-midwifery/scripts/validate_session_schema.py (the DIRECT template — copy module docstring structure, argparse shape, `--json` output contract, collect-all `validate() -> List[str]` pattern, exit-code convention; adapt per PATTERNS lines 20-119)
    - .planning/phases/03-evaluation-hardening/03-CONTEXT.md (D-04 mirror validator verbatim, D-05 presence + cross-references + hardcoded manifest + no YAML parsing, A-3 manifest derived from SKILL.md "Companion references")
    - .planning/phases/03-evaluation-hardening/03-PATTERNS.md (the exact structural mapping from validator → check_package.py: docstring shape, stdlib imports, hardcoded rules-as-data constants, validate() collect-all pattern, main()+argparse+--json+exit-codes, package-root resolution via Path(__file__).resolve().parent.parent)
    - philosophical-midwifery/SKILL.md (the "Companion references" section lines 43-47 = the authoritative cross-reference map to encode in CROSS_REFERENCES per A-3)
    - .planning/phases/02-structured-session-artifacts/02-CONTEXT.md (D-01 stdlib-only, D-02 dual output, D-03 collect-all — the tooling-convention source D-04 binds check_package.py to)
    - .planning/phases/02-structured-session-artifacts/02-01-SUMMARY.md (Phase 2 tooling conventions as shipped — confirm the {valid: bool, errors: [...]} contract and 0/1 exit codes that check_package.py must match)
  </read_first>
  <behavior>
    The completeness check's contract, encoded by running it against the real complete package (GREEN) and a deliberately-broken temp copy (RED) BEFORE finalizing the script body:
    - `python3 philosophical-midwifery/scripts/check_package.py` (no args), run from repo root against the complete package, exits 0 and prints `valid` in default mode.
    - `python3 ... check_package.py --json` exits 0 and prints exactly `{"valid": true, "errors": []}`.
    - When a required file is missing (e.g., temporarily rename `references/EVALUATION_RUBRIC.md` in a temp copy), the check exits 1, default mode prints one human-readable line per missing file, and `--json` mode prints `{"valid": false, "errors": [...]}` whose errors array names the missing path.
    - When an expected internal cross-reference is broken (e.g., remove the string `PATHOLOGOS_PATTERNS.md` from a temp copy of `SKILL.md`), the check exits 1 and the errors array names the file whose reference did not resolve.
    - The check collects ALL problems in one pass before reporting (D-03) — a package with two missing files reports both, not just the first.
    - The check imports ONLY stdlib modules (`argparse`, `json`, `sys`, `pathlib`, `typing`) — no third-party deps (D-01/D-04).
    - The check uses no shell-spawning, no code-execution primitives, and no network calls (mirrors the validator's sandbox-safe posture, threat T-03-01 mitigation).
    - There is no `<path>` positional argument (D-04): the script locates the package root itself as `Path(__file__).resolve().parent.parent` (script at `<pkg>/scripts/check_package.py`).
  </behavior>
  <action>
    Author ONE new file: `philosophical-midwifery/scripts/check_package.py`. Do NOT edit `validate_session_schema.py` or any other Phase 1/2 file. Run `python3` at `/opt/homebrew/bin/python3` (Python 3.14.6; no `pyproject.toml`/`requirements.txt` exists — CONTEXT specifics line 109).

    **D-04 binding: this script is a near-verbatim STRUCTURAL copy of `validate_session_schema.py` (the DIRECT template per PATTERNS lines 20-119).** Copy its module-docstring structure, import block, `validate() -> List[str]` collect-all signature, and `main()` + argparse + `--json` + exit-code block almost verbatim. Only TWO structural departures (per PATTERNS lines 52, 119):
    1. Drop the `<path>` positional argument. The script takes only the optional `--json` flag.
    2. Resolve the package root relative to the script: `Path(__file__).resolve().parent.parent` (script lives at `<pkg>/scripts/check_package.py`, so `.parent` = `scripts/`, `.parent.parent` = the `philosophical-midwifery/` package root).

    **D-01/D-04: stdlib-only.** Import ONLY `argparse`, `json`, `sys`, `pathlib.Path`, and `typing.List` (analog uses `datetime` for ISO-8601; check_package uses `pathlib` instead — PATTERNS line 52). Forbid all shell-spawning, code-execution, and network primitives — phrase the docstring prohibition by concept exactly as the analog does: "performs no network access and uses no code-execution or shell-spawning primitives (stdlib-only by design)". Do NOT import any third-party package (no `yaml`, no `jsonschema`, no `requests`, etc.).

    **D-05: hardcoded manifest + cross-reference map.** Encode the required-file set and cross-reference map as module-level constants (mirrors the validator's `PHASE_VALUES` / `REQUIRED_FIELDS` constants-pattern, PATTERNS lines 62-64):
    - `REQUIRED_FILES`: a tuple/list of paths relative to the package root (the `philosophical-midwifery/` directory). Include EVERY file the complete v1 package must ship: `SKILL.md`; `references/PATHOLOGOS_PATTERNS.md`, `references/QUESTION_TAXONOMY.md`, `references/SAFETY_BOUNDARIES.md`, `references/SESSION_SCHEMA.md`, `references/EVALUATION_RUBRIC.md`; `assets/session_summary_template.md`, `assets/belief_graph_template.json`; `scripts/validate_session_schema.py`, `scripts/check_package.py`; and the five `examples/` fixtures (`examples/normal-inquiry.md`, `examples/overinterpretation-prevention.md`, `examples/advice-avoidance.md`, `examples/high-emotion-slowdown.md`, `examples/safety-boundary-handling.md`). The Phase 2 schema fixtures (`scripts/fixtures/valid.json`, `scripts/fixtures/invalid.json`) MAY be included as required files (they are part of the shipped package).
    - `CROSS_REFERENCES`: a dict mapping a package-relative file path → list of internal-reference strings that file must contain (per A-3, derived from SKILL.md's "Companion references" section, PATTERNS lines 266-271). Encode AT MINIMUM: `SKILL.md` → [`references/PATHOLOGOS_PATTERNS.md`, `references/QUESTION_TAXONOMY.md`, `references/SAFETY_BOUNDARIES.md`] (the three companion references SKILL.md lines 45-47 name verbatim). Add additional cross-references the agent can derive from reading the shipped files (e.g., `SKILL.md` references `references/SAFETY_BOUNDARIES.md` from its "When NOT to use" section; the four Phase 1+2 references cross-link where they actually do). Keep the map conservative — only encode references that genuinely exist in the shipped prose.

    Do NOT parse YAML frontmatter (D-05 explicit deferral — PATTERNS line 31, CONTEXT `<deferred>`).

    **D-03: collect-all `validate(package_root: Path) -> List[str]`.** Accumulate EVERY problem before returning (mirrors analog lines 80-167). Append `f"{path}: missing required file"` for each missing required file, and `f"{path}: missing expected reference '{ref}'"` for each cross-reference that does not appear in the file's text. Use `path.read_text(encoding="utf-8")` to load file contents for the cross-reference substring check; guard with `if path.exists()` so a missing file does not crash the cross-reference pass (it is already reported as missing). Return the error list (empty = valid).

    **D-02: dual output + exit codes (binding — identical ergonomics to the validator).** `main(argv: List[str] = None) -> int`: build an `argparse.ArgumentParser` with description "Check philosophical-midwifery package completeness: required files present and internal cross-references resolve (EVAL-03, D-05)." Add `--json` (`dest="as_json"`, `action="store_true"`, help `'emit a single {"valid": bool, "errors": [...]} document'`). NO positional `path` arg (D-04). Compute `package_root = Path(__file__).resolve().parent.parent`, call `errors = validate(package_root)`. In `--json` mode print `json.dumps({"valid": not errors, "errors": errors})`; in default mode print one line per error then `valid` on success. Return `0 if not errors else 1`. End with `if __name__ == "__main__": sys.exit(main())`.

    Add a module docstring naming the requirement (EVAL-03) and the source of truth (D-05 hardcoded manifest derived from SKILL.md's "Companion references" section, A-3), and citing the binding to the validator's conventions (D-04).

    After writing, RUN the script from repo root against the complete package (GREEN: expect exit 0 + `valid` + `{"valid": true, "errors": []}`). Then verify the RED contract using a temp copy: copy `philosophical-midwifery/` to a temp dir, delete one required file and break one cross-reference, point the script at the temp copy via a small wrapper (or temporarily mutate + revert), and confirm exit 1 + collect-all reporting of BOTH problems. Iterate on the script body until both contracts hold. (The automated `<verify>` below exercises this against the live package by temporarily renaming files; do not leave the package broken.)
  </action>
  <verify>
    <automated>
      # GREEN contract: complete package exits 0 in both modes
      # RED contract: temporarily break the package and confirm exit 1 + collect-all
      # plus stdlib-only / sandbox-safe source gate (D-01/D-04, T-03-01 mitigation)
      python3 - <<'PY'
      import json, os, shutil, subprocess, sys, tempfile
      PY=sys.executable
      V="philosophical-midwifery/scripts/check_package.py"

      def run(args, cwd=None):
          r=subprocess.run([PY,V]+args,capture_output=True,text=True,cwd=cwd)
          return r.returncode,r.stdout,r.stderr

      # GREEN: complete package
      rc,out,err=run([])
      assert rc==0, f"complete package should exit 0, got {rc}; stderr={err}"
      assert out.strip()=="valid", f"default-mode success should print 'valid'; got {out!r}"
      rc,out,err=run(["--json"])
      assert rc==0, f"complete package --json should exit 0, got {rc}"
      j=json.loads(out); assert j=={"valid": True, "errors": []}, f"bad GREEN --json payload: {j}"
      print("GREEN contract OK")

      # RED contract via temp copy: delete one required file + break one cross-reference
      with tempfile.TemporaryDirectory() as td:
          tmp_pkg=os.path.join(td,"philosophical-midwifery")
          shutil.copytree("philosophical-midwifery", tmp_pkg)
          # delete one required file
          os.remove(os.path.join(tmp_pkg,"references","EVALUATION_RUBRIC.md"))
          # break one cross-reference: scrub PATHOLOGOS_PATTERNS.md mentions from SKILL.md
          sk=os.path.join(tmp_pkg,"SKILL.md")
          s=open(sk).read().replace("PATHOLOGOS_PATTERNS.md","BROKEN.md")
          open(sk,"w").write(s)
          # write a wrapper that points validate() at the temp package
          wrapper=os.path.join(td,"wrap_check.py")
          open(wrapper,"w").write(
              "import sys; sys.path.insert(0,'philosophical-midwifery/scripts'); "
              "import check_package; "
              f"check_package.validate(__import__('pathlib').Path('{tmp_pkg}'))"
          )
          # Instead run check_package against temp by importing validate directly
          probe=os.path.join(td,"probe.py")
          open(probe,"w").write(
              "import sys, json\n"
              "sys.path.insert(0,'philosophical-midwifery/scripts')\n"
              "import check_package\n"
              "from pathlib import Path\n"
              f"errs = check_package.validate(Path('{tmp_pkg}'))\n"
              "print(json.dumps({'valid': not errs, 'errors': errs}))\n"
              "sys.exit(0 if not errs else 1)\n"
          )
          r=subprocess.run([PY,probe],capture_output=True,text=True)
          assert r.returncode==1, f"broken package should exit 1; got {r.returncode}; stdout={r.stdout}"
          pj=json.loads(r.stdout)
          assert pj["valid"] is False, f"broken package must report valid:false: {pj}"
          errs=" ".join(pj["errors"]).lower()
          # collect-all: BOTH problems reported in one pass (D-03)
          has_missing = "evaluation_rubric.md" in errs.replace("\\","/").lower()
          has_ref = ("reference" in errs) or ("pathologos_patterns" in errs)
          assert has_missing, f"RED did not report the missing required file; errors={pj['errors']}"
          assert has_ref, f"RED did not report the broken cross-reference; errors={pj['errors']}"
          assert len(pj["errors"])>=2, f"collect-all (D-03) must report >=2 problems; got {pj['errors']}"
      print("RED contract OK (collect-all across missing-file + broken-reference)")

      # stdlib-only / sandbox-safe source gate (D-01/D-04, T-03-01 mitigation)
      src=open(V).read()
      for bad in ["import yaml","import requests","import jsonschema","subprocess","os.system"," eval(","exec(","__import__"]:
          assert bad not in src, f"check_package.py must be stdlib-only and sandbox-safe (D-04); found '{bad}'"
      for needed in ["import argparse","import json","import sys","from pathlib","Path(__file__).resolve().parent.parent"]:
          assert needed in src, f"check_package.py missing expected element: {needed}"
      assert '"valid"' in src and '"errors"' in src, "check_package.py must emit the {valid, errors} --json contract (D-02)"
      assert "def validate(" in src, "check_package.py must define validate() -> List[str] (collect-all, D-03)"
      print("source gate OK (stdlib-only, sandbox-safe, contract present)")
      print("ALL TASK 2 BEHAVIOR CHECKS PASSED")
      PY
    </automated>
  </verify>
  <acceptance_criteria>
    - `philosophical-midwifery/scripts/check_package.py` exists alongside `validate_session_schema.py` and is a near-verbatim structural copy (module docstring, imports, `validate() -> List[str]`, `main()` + argparse + `--json` + exit codes) per D-04 — with only two departures: no `<path>` positional arg, and package-root resolution via `Path(__file__).resolve().parent.parent`.
    - `python3 philosophical-midwifery/scripts/check_package.py` (no args, run from repo root) exits 0 and prints `valid`.
    - `python3 philosophical-midwifery/scripts/check_package.py --json` exits 0 and prints exactly `{"valid": true, "errors": []}`.
    - When run against a package copy with a missing required file AND a broken cross-reference, the script exits 1, reports BOTH problems in one pass (D-03 collect-all), and emits `{"valid": false, "errors": [...]}` whose errors array names the missing path AND the unresolved reference.
    - The script's `REQUIRED_FILES` manifest includes every file the complete v1 package ships (SKILL.md; 5 references incl. EVALUATION_RUBRIC.md; 2 assets; 2 scripts incl. check_package.py itself; 5 examples/*.md) — A-3.
    - The script's `CROSS_REFERENCES` map includes at minimum `SKILL.md → [references/PATHOLOGOS_PATTERNS.md, references/QUESTION_TAXONOMY.md, references/SAFETY_BOUNDARIES.md]` (the three companion references from SKILL.md lines 45-47) — A-3.
    - The script imports ONLY stdlib modules (no `yaml`/`requests`/`jsonschema`); uses no shell-spawning/code-execution/network primitives; phrases the docstring prohibition by concept ("no code-execution or shell-spawning primitives") matching the analog (D-01/D-04, T-03-01 mitigation).
    - The script does NOT parse YAML frontmatter (D-05 explicit deferral).
    - No Phase 1 or Phase 2 file is modified.
  </acceptance_criteria>
  <done>
    check_package.py passes the complete package (exit 0, `valid`, `{"valid": true, "errors": []}`); fails a deliberately-broken copy with collect-all reporting of both the missing required file AND the broken cross-reference (exit 1, `{"valid": false, "errors": [...]}`); is stdlib-only and sandbox-safe; its manifest includes every shipped file and its cross-reference map encodes SKILL.md's three companion references; all automated checks above print "ALL TASK 2 BEHAVIOR CHECKS PASSED".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Human review — rubric voice + 7 dimensions + fixture coverage + safety-stop fidelity + check_package ergonomics; then author the frozen review record + repo-root README</name>
  <files>
    # Read-only review of Phase 3 artifacts (no file modifications during the review itself)
    philosophical-midwifery/references/EVALUATION_RUBRIC.md
    philosophical-midwifery/examples/normal-inquiry.md
    philosophical-midwifery/examples/overinterpretation-prevention.md
    philosophical-midwifery/examples/advice-avoidance.md
    philosophical-midwifery/examples/high-emotion-slowdown.md
    philosophical-midwifery/examples/safety-boundary-handling.md
    philosophical-midwifery/scripts/check_package.py
    # Files written AFTER approval (post-review deliverables)
    .planning/phases/03-evaluation-hardening/03-01-REVIEW.md
    README.md
  </files>
  <read_first>
    - philosophical-midwifery/references/EVALUATION_RUBRIC.md (review: 7 dimensions verbatim + ONE worked example + NO aggregate + soft-modals voice + sibling declaration)
    - philosophical-midwifery/examples/normal-inquiry.md (review: full 8-step loop + recognition-determines-worth thread coherent with the rubric worked example)
    - philosophical-midwifery/examples/overinterpretation-prevention.md (review: candidates as hypotheses; user selects/revises)
    - philosophical-midwifery/examples/advice-avoidance.md (review: no advice/reassurance/essay; questions over assertions)
    - philosophical-midwifery/examples/high-emotion-slowdown.md (review: modulation rule fires; pace slows; inquiry continues — NOT a hard stop)
    - philosophical-midwifery/examples/safety-boundary-handling.md (review: stop → resources → no-auto-resume; EXPLORATORY vs RED-FLAG ruling; fixture data clearly marked)
    - philosophical-midwifery/scripts/check_package.py (review: near-verbatim structural copy of validate_session_schema.py; stdlib-only; manifest + cross-reference map correct)
    - philosophical-midwifery/SKILL.md (verify the rubric + fixtures do not contradict the shipped loop / dialogue rules / companion-references section)
    - philosophical-midwifery/references/SAFETY_BOUNDARIES.md (verify the safety fixture's stop/resources/no-auto-resume matches the 5 conditions + response pattern + resume gate)
    - philosophical-midwifery/references/SESSION_SCHEMA.md (sibling-reference declaration analog for the rubric; voice analog)
    - philosophical-midwifery/references/QUESTION_TAXONOMY.md (soft-modals voice-note analog for the rubric)
    - philosophical-midwifery/scripts/validate_session_schema.py (the direct template — verify check_package.py's structural fidelity per D-04)
    - .planning/PROJECT.md (Constraints + Key Decisions — epistemic humility, examined-proposition atomic unit, v1 = pure Agent Skill package)
    - .planning/phases/03-evaluation-hardening/03-CONTEXT.md (D-01..D-08 + A-1..A-5 — the locked decisions this review enforces)
  </read_first>
  <action>
    Perform a blocking human review of the Phase 3 deliverables across seven checks (detailed in `<how-to-verify>`): (1) rubric voice + soft modals + sibling declaration; (2) 7 dimensions verbatim + ONE worked example + NO aggregate score (D-01/D-02/D-03); (3) 5 fixtures cover the SC2 scenarios with clean transcripts + correct asserted behaviors (D-06, A-1, A-2); (4) safety-boundary fixture fidelity to SAFETY_BOUNDARIES.md's 5 stop conditions + response pattern + no-auto-resume gate (Phase 1 D-15, A-1); (5) recognition-determines-worth cross-file coherence between the rubric's worked example and the normal-inquiry fixture (D-03/A-2/D-12 thread); (6) rubric + fixtures do not contradict SKILL.md's loop/dialogue rules (Phase 2 D-08 continuity); (7) optional CLI spot-run of check_package.py to eyeball ergonomics (D-04). The mechanical behavior is already asserted by Tasks 1–2 automated checks; this checkpoint judges what automation CANNOT: voice, coverage fidelity, cross-file coherence, safety-stop fidelity, and prompt weaknesses that should roll into README Limitations (D-08, A-4). No file modifications occur during the review itself.

    **ON APPROVAL** (after the reviewer types "approved" or describes issues + their disposition), the executor writes TWO post-review deliverables:
    1. **`.planning/phases/03-evaluation-hardening/03-01-REVIEW.md`** — the frozen SC4 review record (D-08). Use a short structure: a one-paragraph summary of what was reviewed, a Findings table (each row: finding, severity, disposition = `fixed-now` | `deferred`, location), and a Sign-off line. Findings marked `fixed-now` are corrected in the relevant file before the review record is frozen (note the fix in the row); findings marked `deferred` roll into the README Limitations section below. If the reviewer approves with zero findings, the record states "No findings; all seven checks passed" and the README Limitations section is empty/omitted.
    2. **`README.md`** (repo root — SC5, D-07) — the collaborator entry point. Sections in this order: (a) one-paragraph "What this is" naming the skill and its core value (do NOT duplicate SKILL.md's YAML `description` verbatim — reference it); (b) "Load the skill" — one short paragraph or list explaining that `philosophical-midwifery/SKILL.md` is the in-package activation doc (its frontmatter `name` + `description` drive activation) and how a user points their agent at it; (c) "Run the checks" — the two commands with identical ergonomics (D-04), copied from PATTERNS lines 222-228:
       ```
       python3 philosophical-midwifery/scripts/validate_session_schema.py <path-to-session.json> [--json]
       python3 philosophical-midwifery/scripts/check_package.py [--json]
       ```
       including a one-line note on what each validates (session-JSON schema vs package completeness) and the `{"valid": bool, "errors": [...]}` `--json` contract + 0/1 exit codes shared by both; (d) "Self-evaluate a session" — one short paragraph pointing at `philosophical-midwifery/references/EVALUATION_RUBRIC.md`, naming the 7 dimensions, and stating scoring is pure-prose/human (D-02) with NO aggregate score (D-03); (e) "Behavioral fixtures" — one short paragraph pointing at `philosophical-midwifery/examples/` and listing the 5 SC2 scenarios the fixtures cover; (f) "Package layout" — a short tree or bulleted list of the package's directories (`SKILL.md`, `references/`, `assets/`, `scripts/`, `examples/`) so a newcomer can navigate; (g) **"Limitations / Known Issues"** (D-08, A-4) — populated from the SC4 review's `deferred` findings; each limitation is one short honest bullet (the known gap + where to find more detail). If the review produced zero deferred findings, state "No known issues at v1 ship time" rather than omitting the section. ALSO include a short "Out of scope for v1" subsection listing the v2-deferred items (local harness HAR-*, belief graph GRPH-*, LLM-judge harness for the rubric) so readers understand what v1 deliberately does NOT do — sourced from REQUIREMENTS.md v2 section + CONTEXT.md `<deferred>`.

    Do NOT create an in-package README (D-07 — would duplicate/drift from SKILL.md frontmatter). The repo-root README is the sole entry point.
  </action>
  <what-built>
    Phase 3's evaluation + hardening layer (Task 1: EVALUATION_RUBRIC.md + 5 examples/*.md fixtures; Task 2: check_package.py). This review gates the v1-readiness sign-off and produces the frozen review record + repo-root README that ship the milestone.
  </what-built>
  <how-to-verify>
    1. **Rubric voice + sibling declaration (D-01, A-5):** Read `references/EVALUATION_RUBRIC.md`. Confirm it opens with a sibling declaration (joins the four Phase 1+2 references, does not contradict `../SKILL.md`) and a soft-modals voice note (addresses the scorer; *prefer, consider, look for*). Flag any hard modal (MUST/NEVER) that is not a direct quote — those belong in SAFETY_BOUNDARIES.md only.
    2. **7 dimensions + ONE worked example + NO aggregate (D-01/D-02/D-03):** Confirm the rubric documents exactly the 7 design-draft §13 dimensions in the correct order (proposition clarity, user ownership, dialectical discipline, logical pressure, non-coercion, summary quality, practical usefulness) with the 1–5 scale, contains exactly ONE worked scoring example, computes NO aggregate/composite/total, and declares itself pure-prose/human-scored (no mechanical scoring in v1).
    3. **5 fixtures cover SC2 with clean transcripts (D-06, A-1, A-2):** Read each `examples/*.md`. Confirm: 5 scenarios covered (normal inquiry, overinterpretation prevention, advice avoidance, high-emotion slowdown, safety-boundary handling); each opens with a framing paragraph; transcripts are CLEAN (no inline "Score:"/"Rubric:" annotations); each demonstrates the DESIRED behavior for its scenario. Flag any scenario that is ambiguous or fails to show the asserted behavior — that is a candidate for either a fix-now (revise the transcript) or a deferred limitation (note in README).
    4. **Safety-boundary fidelity (Phase 1 D-15, A-1):** Read `examples/safety-boundary-handling.md` against `references/SAFETY_BOUNDARIES.md`. Confirm the fixture demonstrates the FULL response pattern: acknowledge → STOP probing → state the skill does not provide crisis care/therapy/diagnosis → offer curated resources (988, findahelpline.com) → ASK locale if unknown → MUST NOT auto-resume. Confirm the red-flag content is clearly marked as FIXTURE DATA (cannot be misread as instruction — threat T-03-03 mitigation).
    5. **Recognition-determines-worth cross-file coherence (D-03/A-2/D-12 thread):** Confirm the rubric's worked example and `examples/normal-inquiry.md` derive from the same recognition-determines-worth thread; the normalized proposition ("The value of work depends on recognition") and revised proposition ("Recognition affects visibility and opportunity, but does not determine intrinsic worth") should match across both files.
    6. **No contradiction with SKILL.md (Phase 2 D-08 continuity):** Confirm the rubric's guidance and the fixtures' agent behavior align with (do not contradict) the 8-step inquiry loop + dialogue rules + companion-references section in `philosophical-midwifery/SKILL.md`.
    7. (Optional) Spot-run for ergonomics (D-04): `python3 philosophical-midwifery/scripts/check_package.py` (expect exit 0 + `valid`) and `... check_package.py --json` (expect `{"valid": true, "errors": []}`) to confirm the CLI ergonomics match `validate_session_schema.py`.
  </how-to-verify>
  <verify>
    <human-check>
      Reviewer confirms all seven checks pass (or flags specific findings with a fixed-now vs. deferred disposition). This is a blocking human gate — no automated assertion can substitute for the voice/coverage/coherence/safety-fidelity judgment. The reviewer's `deferred` findings populate the README "Limitations" section (D-08, A-4); `fixed-now` findings are corrected before the review record is frozen.
    </human-check>
    <automated>
      # Post-approval gate: confirm BOTH post-review deliverables were written and are well-formed
      # (run by the executor after the reviewer types "approved" and any fixed-now edits land)
      python3 - <<'PY'
      import os
      rev=".planning/phases/03-evaluation-hardening/03-01-REVIEW.md"
      assert os.path.isfile(rev), f"frozen review record missing: {rev}"
      r=open(rev).read()
      assert "Review" in r, "review record must open with a review heading/summary"
      assert "finding" in r.lower() or "no findings" in r.lower(), "review record must list findings or state 'No findings'"
      assert "sign-off" in r.lower() or "approved" in r.lower(), "review record must include a sign-off/approval line"
      rd=open("README.md").read()
      # D-07 mandatory sections
      for section in ["philosophical-midwifery","validate_session_schema.py","check_package.py","EVALUATION_RUBRIC.md","examples"]:
          assert section in rd, f"README missing expected reference: {section}"
      # both check commands documented (D-04 identical ergonomics)
      assert "validate_session_schema.py" in rd and "check_package.py" in rd, "README must document both check commands"
      assert "988" in rd or "findahelpline" in rd or "Limitations" in rd, "README must address safety resources OR Limitations"
      # D-07: NO in-package README (only repo-root)
      assert not os.path.isfile("philosophical-midwifery/README.md"), "must NOT create an in-package README (D-07)"
      print("ALL TASK 3 POST-APPROVAL CHECKS PASSED")
      PY
    </automated>
  </verify>
  <done>
    Reviewer types "approved" (or describes findings + their fixed-now/deferred disposition). On approval: (a) any `fixed-now` findings are corrected in the relevant files; (b) the executor writes `.planning/phases/03-evaluation-hardening/03-01-REVIEW.md` (frozen SC4 record listing every finding with its disposition + a sign-off line — D-08); (c) the executor writes repo-root `README.md` covering what-the-skill-is / load / run-both-checks / self-evaluate / fixtures / package-layout / Limitations (populated from deferred findings, D-07/D-08/A-4) + Out-of-scope-for-v1; (d) NO in-package README is created (D-07). The automated post-approval gate prints "ALL TASK 3 POST-APPROVAL CHECKS PASSED". On approval, the executor writes the Phase 3 summary and the milestone is v1-ready.
  </done>
  <resume-signal>
    Type "approved" if all seven checks pass (the executor then writes the review record + README and the phase is complete). Or describe which check failed, the finding, and whether to fix-now (executor revises the file before freezing the review) or defer (finding rolls into README Limitations). On approval, the executor writes the Phase 3 summary.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → check_package.py | A human runs `python3 philosophical-midwifery/scripts/check_package.py [--json]`; the script locates the package root itself (no path arg, D-04) and reads the package's files. |
| check_package.py → package files | The script reads multiple files at known paths under `philosophical-midwifery/` to verify presence + cross-references. |
| reviewer → safety fixture | A human reads `examples/safety-boundary-handling.md`, which contains red-flag phrase fixtures (e.g., "I just want it to end") as illustrative data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Elevation | `check_package.py` is a new code-execution surface in the package (sibling to Phase 2's validator) | mitigate | D-04 stdlib-only constraint (mirrors T-02-01); task action forbids shell-spawning/code-execution/network primitives and phrases the docstring prohibition by concept ("no code-execution or shell-spawning primitives") matching the analog; the script reads files at hardcoded package-relative paths only and prints file paths + reference names only. Automated gate in Task 2 `<verify>` asserts no dangerous APIs appear in source. |
| T-03-02 | Information Disclosure | check_package.py output could echo file contents | accept | Output is limited to file paths + reference-name strings (no file body is printed); the package contains only synthetic recognition-determines-worth + fixture content (no PII, no secrets). Low value, low risk. |
| T-03-03 | Tampering / Misuse | Safety fixture `examples/safety-boundary-handling.md` contains red-flag phrases as illustrative data; could be misread as instruction or as a real cry for help | mitigate | Task 1 action requires the red-flag content be clearly marked as FIXTURE DATA demonstrating the desired STOP response (not a real cry for help); the transcript shows the agent correctly stopping and offering resources, so the fixture models the safe behavior rather than the unsafe trigger. Task 3 review check #4 verifies the marking is unambiguous. |
| T-03-04 | Repudiation | n/a — no audit log in v1 | accept | check_package.py is a local dev tool; no multi-user audit requirement. |
| T-03-SC | Tampering (supply chain) | npm/pip/cargo package installs | accept (N/A) | D-04 mandates stdlib-only — there are NO package-manager installs in this phase. No `[ASSUMED]`/`[SUS]` packages to gate. |
</threat_model>

## Artifacts this phase produces

Files (all NEW — no Phase 1 or Phase 2 file is edited; Phase 2 D-08 additive-only continuity):

Under `philosophical-midwifery/`:
- `philosophical-midwifery/references/EVALUATION_RUBRIC.md` — pure-prose human-scored rubric over the 7 design-draft §13 dimensions + 1–5 scale + ONE recognition-determines-worth worked example + NO aggregate score (EVAL-01, D-01/D-02/D-03, A-5).
- `philosophical-midwifery/examples/normal-inquiry.md` — golden full-loop inquiry transcript (recognition-determines-worth thread) — SC2 scenario 1 (EVAL-02, D-06, D-12 thread).
- `philosophical-midwifery/examples/overinterpretation-prevention.md` — golden transcript: candidates as hypotheses + user selects/revises — SC2 scenario 2.
- `philosophical-midwifery/examples/advice-avoidance.md` — golden transcript: no advice/reassurance/essay — SC2 scenario 3.
- `philosophical-midwifery/examples/high-emotion-slowdown.md` — golden transcript: SKILL.md modulation rule fires, pace slows — SC2 scenario 4.
- `philosophical-midwifery/examples/safety-boundary-handling.md` — golden transcript: stop → resources → no-auto-resume (Phase 1 D-15) — SC2 scenario 5.
- `philosophical-midwifery/scripts/check_package.py` — stdlib-only package-completeness check: required-file presence + internal cross-reference resolution; collect-all reporting; dual CLI/`--json` output (EVAL-03, D-04/D-05, A-3).

At repo root:
- `README.md` — collaborator entry point: what-the-skill-is / load / run-both-checks / self-evaluate / fixtures / package-layout / Limitations + Out-of-scope-for-v1 (SC5, D-07/D-08, A-4).

Under `.planning/phases/03-evaluation-hardening/`:
- `03-01-REVIEW.md` — frozen SC4 human-review record: findings table (fixed-now vs. deferred) + sign-off (SC4, D-08).

Symbols / identifiers this phase introduces:

- **CLI flag:** `--json` on `check_package.py` → emits `{"valid": bool, "errors": [...]}` (D-02/D-04 — identical ergonomics to `validate_session_schema.py`).
- **`check_package.py` module constants (D-05, A-3):**
  - `REQUIRED_FILES` — tuple/list of package-relative paths every complete v1 package must ship: `SKILL.md`; `references/{PATHOLOGOS_PATTERNS,QUESTION_TAXONOMY,SAFETY_BOUNDARIES,SESSION_SCHEMA,EVALUATION_RUBRIC}.md`; `assets/{session_summary_template.md, belief_graph_template.json}`; `scripts/{validate_session_schema.py, check_package.py}`; `examples/{normal-inquiry,overinterpretation-prevention,advice-avoidance,high-emotion-slowdown,safety-boundary-handling}.md`.
  - `CROSS_REFERENCES` — dict mapping file → required internal-reference substrings; at minimum `SKILL.md → [references/PATHOLOGOS_PATTERNS.md, references/QUESTION_TAXONOMY.md, references/SAFETY_BOUNDARIES.md]` (verbatim from SKILL.md lines 45-47).
- **Package-root resolution idiom:** `Path(__file__).resolve().parent.parent` (script at `<pkg>/scripts/check_package.py` → package root).
- **Rubric dimensions (canonical, verbatim from design-draft §13):** proposition clarity, user ownership, dialectical discipline, logical pressure, non-coercion, summary quality, practical usefulness — 1–5 scale, NO aggregate (D-03).
- **Fixture scenario set (canonical from ROADMAP SC2):** normal inquiry, overinterpretation prevention, advice avoidance, high-emotion slowdown, safety-boundary handling.
- **README section set (D-07):** What-this-is / Load-the-skill / Run-the-checks / Self-evaluate / Behavioral-fixtures / Package-layout / Limitations / Out-of-scope-for-v1.

<verification>
**Automated (per task):**
- Task 1: python heredoc asserts all 7 dimensions + 1–5 scale + ONE recognition-determines-worth worked example + NO aggregate + sibling declaration + soft-modals voice + pure-prose/human-scored statement in EVALUATION_RUBRIC.md; asserts examples/ contains exactly the 5 expected kebab-case .md files, each with Agent/User dialogue + framing + ≥15 lines + no inline rubric annotations; asserts safety fixture uses EXPLORATORY/RED-FLAG + references 988/findahelpline + demonstrates no-auto-resume; asserts normal-inquiry fixture carries the recognition-determines-worth thread + canonical normalized proposition.
- Task 2: python heredoc runs check_package.py against the complete package (GREEN: exit 0, `valid`, `{"valid": true, "errors": []}`); runs against a deliberately-broken temp copy (RED: exit 1, collect-all of missing-file + broken-reference); asserts the {valid: bool, errors: [...]} contract, the `validate() -> List[str]` collect-all pattern, package-root resolution via `Path(__file__).resolve().parent.parent`, and the stdlib-only/sandbox-safe source gate.
- Task 3: post-approval python heredoc asserts `.planning/phases/03-evaluation-hardening/03-01-REVIEW.md` exists with findings + sign-off, README.md exists with all D-07 sections + both check commands + safety-resource/Limitations mention, and NO in-package README was created.
- Phase 1+2 untouched: `git diff --name-only HEAD~..HEAD` across the phase shows ONLY the 8 new files + the in-.planning/ review record (asserted in each task's `git status` check).

**Human (Task 3 blocking checkpoint):** rubric voice + 7 dimensions + ONE worked example + NO aggregate; 5-fixture coverage fidelity; safety-stop fidelity to SAFETY_BOUNDARIES.md's 5 conditions + response pattern + resume gate (D-15); recognition-determines-worth cross-file coherence; non-contradiction with SKILL.md; check_package ergonomics spot-run.

**Cross-phase (deferred to v2):** LLM-judge harness for automated rubric scoring (HAR-* scope); mechanical behavioral assertions; per-scenario paired failure examples; YAML frontmatter validation; structured behavioral-fixture format. The 8 files above land at the paths v2 expects.
</verification>

<success_criteria>
Mapped to ROADMAP §"Phase 3: Evaluation & Hardening" Success Criteria:

1. ✅ Evaluation guidance scores sessions on proposition clarity, user ownership, dialectical discipline, logical pressure, non-coercion, summary quality, and practical usefulness — Task 1 (EVAL-01, D-01/D-02/D-03).
2. ✅ Fixtures/examples cover normal inquiry, overinterpretation prevention, advice avoidance, high-emotion slowdown, and safety-boundary handling — Task 1 (EVAL-02, D-06, A-1).
3. ✅ A lightweight package-completeness check confirms required files are present and internally referenced — Task 2 (EVAL-03, D-04/D-05, A-3).
4. ✅ Manual review notes identify any remaining prompt weaknesses before the skill is considered v1-ready — Task 3 (SC4, D-08) → frozen `03-01-REVIEW.md`.
5. ✅ README or usage notes explain how to run validation and how to test the skill manually — Task 3 (SC5, D-07/D-08, A-4).

Plus cross-cutting: Phase 1+2 package untouched (Phase 2 D-08 continuity); recognition-determines-worth thread coherent across rubric worked example + normal-inquiry fixture (D-03/A-2/D-12 thread); all 3 requirement IDs {EVAL-01, EVAL-02, EVAL-03} covered; v1 milestone complete on Task 3 approval.
</success_criteria>

<output>
Create `.planning/phases/03-evaluation-hardening/03-01-SUMMARY.md` when done (after Task 3 approval + post-approval writes land). Use the GSD summary template; record the 8 files created + the in-.planning/ review record, requirements completed (EVAL-01, EVAL-02, EVAL-03), the locked decisions implemented (D-01..D-08, A-1..A-5), the recognition-determines-worth coherence as the key cross-phase pattern, and the v1 milestone sign-off.
</output>
